---
scope: Fonctionnalité Evidence Upload (dépôt de preuves de transaction & de litige) — slice ajoutée à la plateforme Escrow B2B ZLECAf
purpose: build-substrate + doc HTML pédagogique
altitude: feature
updated: 2026-07-15T23:43
---

- (constraint) Backend = Spring Boot 3.x / Java 17-21, paradigme en couches package-by-layer sous com.zlecaf.escrow : domain(JPA)/repository/service/web(+dto)/security/config. Injection par constructeur. Controllers minces; la couche service détient toutes les frontières @Transactional.
- (constraint) Entités JPA : @Entity @Table(name=snake_case_plural), @GeneratedValue(IDENTITY) sur BIGSERIAL, colonnes snake_case via @Column(name), TIMESTAMPTZ, @Version pour verrou optimiste. Enums stockés en VARCHAR(50).
- (constraint) DTOs : records imbriqués dans une classe holder (ex. EvidenceDtos) ; requêtes XxxRequest (Bean Validation), réponses XxxDto avec static from(Entity). Enveloppe d'erreur globale {timestamp,status,error,message} via GlobalExceptionHandler + ApiExceptions (NotFound=404, BadRequest=400, Forbidden=403, Conflict=409).
- (constraint) Migrations = Flyway V<n>__desc.sql dans resources/db/migration ; spring.jpa.hibernate.ddl-auto=none (schéma détenu par Flyway). La table evidence_files => nouvelle migration V2__evidence_files.sql, idiomes de V1 (BIGSERIAL PK, TIMESTAMPTZ DEFAULT now(), FK REFERENCES, CREATE INDEX sur transaction_id).
- (constraint) Authentification : JWT stateless. Le principal AuthPrincipal(userId,email,role) est obtenu en controller via @AuthenticationPrincipal. Toutes routes authenticated() sauf /auth, /webhooks/incoming, health, swagger.
- (constraint) Contrôle d'appartenance déjà implémenté côté service : resolveRole(actor,tx) et authorizeView(actor,tx) lèvent ForbiddenException si l'appelant n'est ni ADMIN ni buyer/seller de la transaction. C'est le point unique anti-IDOR à réutiliser (FR-11).
- (constraint) Frontend = Vue 3 (Composition API) + Pinia + Vite/PWA + Tailwind. Updates optimistes via flags sur l'objet transaction (_queuedOffline, _queuedEvent). File hors-ligne actuelle (offlineQueue store) = localStorage + descriptions JSON {method,url,data,meta}, rejeu séquentiel avec break sur toute erreur.
- (constraint) PRD/addendum figés (status final) : 16 FR + 6 NFR. Machine à états : INITIATED->FUNDS_LOCKED->SHIPPED->RELEASED ; branches DISPUTED->RELEASED/REFUNDED. OPEN_DISPUTE autorisé depuis FUNDS_LOCKED et SHIPPED.
- (event) Brownfield code investigation complete (backend + frontend conventions ratified)
- (decision) [ADOPTED] Audit des preuves = réutiliser audit_logs en enrichissant le payload JSONB (action:EVIDENCE_ADDED|EVIDENCE_WITHDRAWN, evidenceId, sha256), écrit par AuditService.recordSuccess (MANDATORY, même transaction). Binds: FR-13, tous endpoints preuves. Prevents: divergence de schéma d'audit / audit hors-transaction. Pas de colonne action_type.
- (decision) [ADOPTED] Anti-IDOR = un unique contrôle d'appartenance (resolveRole/authorizeView côté service) invoqué par TOUS les endpoints preuves avant lecture/écriture/téléchargement. Binds: FR-1,FR-5,FR-7,FR-9,FR-11,FR-12. Prevents: référence directe d'objet non contrôlée, deux endpoints avec des règles d'accès divergentes.
- (decision) [ADOPTED] Interface EvidenceStorage isolant l'accès binaire des endpoints (put/get/delete-logique par clé opaque). Binds: NFR-1, tous dépôts/téléchargements. Prevents: couplage des controllers au backend de stockage, migration future impossible sans casser le contrat d'API.
- (decision) [ADOPTED] HMAC entrant partenaire = réutiliser l'util HmacSigner (comparaison constant-time) + en-tête X-Escrow-Signature, MAIS clé dédiée entrante par partenaire (key-id, ≠ secret webhook sortant), signature couvrant corps+métadonnées+timestamp, et nonce anti-rejeu stocké/vérifié. Binds: FR-5, NFR-5. Prevents: rejeu d'une requête interceptée, réutilisation du secret sortant en entrée.
- (decision) Endpoint composite d'ouverture de litige : POST /api/v1/escrow/{id}/dispute (multipart fichier+commentaire) ouvre le litige ET attache la 1re preuve dans UNE seule unité @Transactional. Binds: FR-6, FR-15. Prevents: litige ouvert sans preuve (fenêtre non-atomique), rejeu offline en deux entrées désynchronisables.
- (decision) Stockage POC = MinIO (S3-compatible) dès maintenant, derrière EvidenceStorage, conteneur ajouté à docker-compose. DIVERGE du PRD §2/NFR-1 ("disque local POC") au profit de la tech-stack cible. Binds: NFR-1. Prevents: refonte de stockage entre POC et prod. NOTE: contradiction PRD à réconcilier.
- (decision) File hors-ligne PWA migrée de localStorage vers IndexedDB pour porter le Blob binaire (≤10 Mo). L'ouverture de litige offline = UNE entrée de file portant OPEN_DISPUTE + binaire(s), rejouée en multipart vers l'endpoint composite. Binds: FR-15, NFR-4. Prevents: dépassement de quota localStorage, perte silencieuse de fichier.
- (decision) Réconciliation à la synchro (remplace le break aveugle de flush()) : classer l'échec — transitoire (réseau / 5xx) => conserver en file et re-tenter ; permanent (4xx, ex. transaction déjà résolue) => annuler l'affichage optimiste, CONSERVER le binaire, notifier l'utilisateur avec motif+état réel, stopper l'auto-retry de cette entrée. Binds: FR-16. Prevents: boucle de retry infinie sur un 4xx, perte silencieuse d'un fichier en attente, état optimiste mensonger.
- (decision) Horodatage : created_at = heure serveur à la réception (source de vérité, ordre chronologique FR-10) ; l'heure client de capture est conservée dans le payload d'audit pour les dépôts offline différés. Binds: FR-10. Prevents: ordre chronologique faussé par des horloges clientes divergentes.
- (decision) Content-sniffing (FR-3/NFR-2) = Apache Tika pour détecter le MIME réel, confronté à extension + Content-Type déclaré ; rejet 400 si incohérence ou type hors {image/jpeg,image/png,application/pdf}. Ajout dépendance org.apache.tika:tika-core. Binds: FR-3, NFR-2.
- (decision) Réconciliation PRD : PRD §2/NFR-1 + addendum §2/§7 mis à jour pour acter MinIO/S3-compatible en périmètre POC (le disque local n'est plus la cible). Seul le chiffrement par fichier au repos reste hors-périmètre/évolution.
- (event) PRD + addendum reconciled with MinIO storage decision
- (version) Existant (ratifié) : Spring Boot 3.3.5, Java 21, PostgreSQL 16, RabbitMQ 3.13, Flyway (+flyway-database-postgresql), hypersistence-utils-hibernate-63 (support @Type JsonBinaryType pour jsonb), springdoc-openapi, jjwt.
- (version) Nouveau (vérifié web 2026-07-15) : org.apache.tika:tika-core 3.3.1 (stable ; 4.0 en beta) pour content-sniffing FR-3/NFR-2.
- (version) Nouveau (vérifié web 2026-07-15) : software.amazon.awssdk:s3 2.47.6 (AWS SDK Java v2 ; v1 EOL 31/12/2025) comme client S3 portable pointé sur MinIO.
- (version) Nouveau (vérifié web 2026-07-15) : image conteneur minio/minio:RELEASE.2025-10-15T17-29-55Z. NOTE risque : dépôt OSS MinIO archivé avr. 2026 ; mitigé par l'abstraction EvidenceStorage + client S3 standard (bascule Garage/SeaweedFS/AWS S3 bon marché).
- (question) [DEFERRED] Spring Boot 3.3.5 : la ligne 3.5 est EOL depuis 30/06/2026 (stable actuelle = 4.1.0). Montée de version = dette hors-périmètre de cette feature ; à planifier séparément.
- (event) Reviewer gate: rubric (1 CONCERN test-strategy + 2 nits) + adversarial (7 holes H1-H7). Applying fixes, AD ids stable.
- (decision) [AMEND AD-1] (fix H1+H2) Endpoint composite accepte files[] 1..N (pas 1 seul) + champ multipart optionnel clientCapturedAt (ISO-8601). La 1re pièce valide le plancher FR-6. Aligne la cardinalité sur AD-9 (le(s) binaire(s)) et donne un canal à l'heure client attendue par AD-5/AD-11.
- (decision) [AMEND AD-4] (fix H5) Plancher FR-6 = ≥1 pièce ACTIVE **toutes parties confondues** sur la transaction (buyer/seller/admin/carrier). Un retrait DISPUTED est refusé s'il amène ce total à 0. Décision POC : une preuve partenaire/adverse compte dans le plancher.
- (decision) [AMEND AD-7] (fix H6 + altitude) Invariant = détecter le MIME réel (content-sniffing) et le confronter à extension+Content-Type (impl Apache Tika, cf Stack). La limite 10 Mo est arbitrée par le service : la limite multipart Spring est réglée > 10 Mo ET MaxUploadSizeExceededException est mappée -> 400 dans le handler global, pour une enveloppe d'erreur uniforme (jamais 413/500).
- (decision) [AMEND AD-8] (fix H4) Anti-rejeu partenaire figé : fenêtre timestamp = ±5 min ; unicité du nonce **scindée par key-id** (clé composite (key_id,nonce)) ; rétention des nonces ≥ durée de fenêtre (purge > 5 min interdite). Rejet si nonce déjà vu pour ce key-id OU timestamp hors fenêtre.
- (decision) [AMEND AD-10] (fix H3) Classification de l'échec de rejeu par **code applicatif dans l'enveloppe d'erreur**, pas par la classe HTTP. Transitoire : offline/réseau, 5xx, 408, 429, et 409 optimistic-lock (@Version). Permanent : ensemble énuméré (ex. DISPUTE_ALREADY_RESOLVED, EVIDENCE_INVALID, WINDOW_CLOSED). 409 est ambigu -> discriminé par le code applicatif.
- (decision) [NEW convention] (fix H7) Contrat multipart partagé figé : champ fichier = files[] (1..N), commentaire = comment (texte), heure capture = clientCapturedAt (optionnel, ISO-8601). Mêmes noms sur composite /dispute, plain /evidence, et /partner (client offline replay inclus).
- (decision) [NEW section] Invariants de test : AD-2 étend EscrowStateMachineTest (garde de fenêtre d'état) ; AD-8 étend HmacSignerTest (nonce+timestamp+fenêtre) ; AD-1 exige un test de rollback transactionnel (échec pièce => pas de transition DISPUTED, aucune ligne evidence_files).
- (decision) [FIX nit] Retirer NFR-6 de binds: (il est déféré, lié à aucun AD).
- (event) spine finalized
- (event) Rendering: ARCHITECTURE-EXPLAINED.html (doc pédagogique) produit. Workflow architecture terminé.
