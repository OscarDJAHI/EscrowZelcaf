# Deferred work — ledger

> **Balayage 2026-07-27** (action item #4 de la rétrospective Epic 1). Rapport complet :
> [`deferred-work-sweep-2026-07-27.md`](./deferred-work-sweep-2026-07-27.md).
>
> **Le compte annoncé était faux : 59 entrées ouvertes et non 10.** Cause : deux formats
> cohabitaient — les entrées POC portent un champ `status:`, les sections production étaient
> de simples puces sans statut, donc invisibles à tout `grep "status: OUVERT"`. Les 17 reports
> POC des lignes 140-209 n'avaient jamais été comptés.
>
> **Toutes les entrées portent désormais un `status:`** (92 au total). Un balayage ultérieur
> peut donc compter juste — et **doit le rester** : toute nouvelle entrée écrit son statut,
> quel que soit son format.
>
> Statuts en usage : `OUVERT` · `RÉSOLU` · `ACCEPTÉ` (risque assumé) · `MITIGÉ` (partiel,
> résiduel routé) · `SKIP` (écarté, sans valeur de production).

# Deferred Work

Ledger of real issues surfaced during review but deliberately not addressed in the
story that found them. Append-only.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-fondations-de-stockage-des-preuves.md`
  summary: La table `evidence_files` n'a aucune contrainte d'intégrité au-delà des NOT NULL et des FK — pas de CHECK sur les valeurs d'enum, pas de cohérence d'attribution, pas de cohérence du retrait, pas d'unicité de `storage_key`.
  evidence: Le schéma livré est conforme à l'ERD figé de l'architecture, qui ne prescrit aucune de ces contraintes — d'où le report plutôt qu'un correctif. Mais les invariants sont réels et documentés en javadoc sans être tenus : (1) une écriture hors application peut stocker `'CARRIER'` → `@Enumerated(STRING)` lève `IllegalArgumentException` à *chaque* relecture, empoisonnant les listes de la Story 1.3 ; (2) rien n'empêche une ligne `CARRIER_PARTNER` sans `partner_company_id`, ni une ligne `BUYER` qui en porte un → preuve inattribuable, précisément dans la table qui fonde l'arbitrage ; (3) rien n'empêche `status = WITHDRAWN` avec `withdrawn_at`/`withdrawn_by_user_id` nuls → retrait sans trace ; (4) `storage_key` n'est pas UNIQUE alors que la relation métadonnée↔objet est voulue 1:1. Décision de niveau architecture (ERD), à trancher avant qu'Epic 2 et 3 n'écrivent ces lignes.
  status: RÉSOLU 2026-07-16 — tranché au niveau architecture (nouvel AD-12 dans ARCHITECTURE-SPINE.md) et implémenté en migration `V3__evidence_files_constraints.sql` : CHECK `uploader_type`/`status`, CHECK d'attribution (CARRIER_PARTNER ⟺ société∧¬user ; humain ⟺ user∧¬société), CHECK de retrait (ACTIVE ⟺ champs de retrait nuls ; WITHDRAWN ⟺ non-nuls), UNIQUE(storage_key). Validé : 37/37 tests verts avec V3 appliquée sur Postgres Testcontainer.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-fondations-de-stockage-des-preuves.md`
  summary: Aucun test automatisé ne vérifie que l'entité `EvidenceFile` correspond bien à la table créée par Flyway.
  evidence: `ddl-auto: none` : Hibernate ne valide pas le mapping au démarrage, et le seul test de la story contourne Spring pour ne prouver que le round-trip de stockage. La correspondance entité↔schéma a été vérifiée manuellement (`\d evidence_files` contre l'entité : les 14 colonnes concordent), mais rien ne la défend contre une régression — un `@Column(name)` erroné ou une migration divergente ne se révélerait qu'à l'exécution, dans la Story 1.2. Un `@DataJpaTest` sur conteneur Postgres (save/find) refermerait le trou ; il arrivera naturellement avec le chemin d'écriture de la Story 1.2.
  status: RÉSOLU en Story 1.2 — `EvidenceServiceTest` est un `@DataJpaTest` sur Postgres Testcontainer qui persiste et relit une ligne `evidence_files` (save/find sur les 14 colonnes), fermant le trou entité↔schéma.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-2-deposer-une-piece-sur-une-transaction.md`
  summary: Le stockage objet n'est pas transactionnel : un échec en milieu de lot après un `EvidenceStorage.store()` réussi laisse des objets MinIO orphelins, et les PUT S3 s'exécutent dans la transaction DB (connexion retenue le temps des I/O).
  evidence: Dans `EvidenceService.deposit` (Pass 2), `store()` écrit le binaire avant `evidenceFiles.save()` et l'audit ; le `@Transactional` ne rollback que la partie JDBC. Si une écriture DB ultérieure échoue (ex. lot de 2 fichiers, le 2e viole une contrainte), la transaction annule les lignes mais les objets déjà stockés restent, sans ligne les référençant — espace perdu, jamais collecté. Les deux patchs de la revue 1.2 (verrou pessimiste + troncature du nom à 255) suppriment le déclencheur concret le plus probable (le 500 sur nom trop long), mais la limitation de fond demeure. Correctif propre = ajouter `delete(storageKey)` au port `EvidenceStorage` + une `TransactionSynchronization afterCompletion` best-effort qui purge les clés stockées sur rollback, ou déplacer les `store()` hors de la frontière transactionnelle. Différé : c'est une décision de niveau architecture (surface du port figée en 1.1) et best-effort par nature (le `delete` peut lui-même échouer) ; cohérent avec la posture POC (aucun cycle de vie, rétention illimitée, pas d'AV). À traiter avant que le volume de production ne rende l'orphelinage coûteux.
  status: RÉSOLU 2026-07-16 (action item #1 de la rétro Epic 1, avant Story 2.1) — AD-6 amendé : ajout `delete(storageKey)` au port (idempotent, no-op sur clé absente), implémenté en S3 `DeleteObjectRequest` ; `EvidenceService.deposit` enregistre un `TransactionSynchronization.afterCompletion` qui `delete()` best-effort les clés écrites si la transaction rollback (échec avalé + loggé, ne masque pas la cause). Tests : contrat `delete` couvert sur vrai MinIO (suppression + no-op sur clé absente), 55/55 verts. Le nettoyage bénéficie automatiquement à l'ouverture composite atomique de la Story 2.1 (même service).

- source_spec: `_bmad-output/implementation-artifacts/spec-1-2-deposer-une-piece-sur-une-transaction.md`
  summary: `files[]` n'a pas de borne supérieure sur le nombre de fichiers, et la validation (Pass 1) bufferise tous les octets en mémoire (borné seulement par le plafond de requête de 60 Mo).
  evidence: Un déposant peut remplir une requête de 60 Mo avec des centaines de petits fichiers valides ; `EvidenceService` tient tous les `byte[]` simultanément dans `List<byte[]> contents`, puis les clone au `store()` — soit ~2× la taille de requête résidente par requête en vol, × la concurrence. Surface d'amplification mémoire/DoS légère, tolérable pour un POC mono-instance à faible concurrence. Correctif = plafonner `files.size()` (ex. ≤ 20) et/ou streamer vers le stockage plutôt que buffériser. Différé comme durcissement.
  status: RÉSOLU 2026-07-16 (Story 5.1 durcissement) — corrigé mais non marqué par la boucle. Bundle livré : plafond files[] (PlatformLimits), listes bornées (preuves + audit), audit EVIDENCE_DOWNLOADED, mapping EvidenceStorageException→enveloppe JSON, withdrawnAt/withdrawnBy au DTO, course loadSeq côté store. Build 110/110 vert.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-consulter-la-liste-chronologique-des-preuves.md`
  summary: `GET .../evidence` (`EvidenceService.list`) matérialise et sérialise **toutes** les lignes `evidence_files` d'une transaction sans borne ni pagination.
  evidence: `findByTransactionIdOrderByCreatedAtAscIdAsc` retourne la totalité des pièces d'une transaction, mappées en `EvidenceDto` puis sérialisées d'un bloc. Pour un POC (peu de pièces par dossier, mono-instance), c'est acceptable et la spec l'a explicitement placé hors périmètre (« aucune pagination »). Mais la borne est absente : un dossier très fourni (rejeu offline massif de l'Epic 4, ou accumulation sur un litige long) charge tout en mémoire et gonfle la réponse. Même limitation que la lecture non bornée d'`EscrowService.getDetail` (trail d'audit complet). Correctif = pagination (`Pageable` + `Page<EvidenceDto>`) ou plafond, à introduire avant que le volume par transaction ne devienne réaliste — et à décider de concert avec la lecture d'audit pour un contrat de liste cohérent sur toute la plateforme. Différé comme durcissement de niveau contrat d'API.
  status: RÉSOLU 2026-07-16 (Story 5.1 durcissement) — corrigé mais non marqué par la boucle. Bundle livré : plafond files[] (PlatformLimits), listes bornées (preuves + audit), audit EVIDENCE_DOWNLOADED, mapping EvidenceStorageException→enveloppe JSON, withdrawnAt/withdrawnBy au DTO, course loadSeq côté store. Build 110/110 vert.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-4-telecharger-une-preuve-en-toute-securite.md`
  summary: Le téléchargement d'une preuve (`GET .../evidence/{eid}/download`) n'écrit aucune entrée d'audit, alors que le dépôt en écrit une par pièce — le chemin de lecture du binaire est un trou dans la piste d'audit.
  evidence: Dans un domaine de séquestre/litige, *qui* a accédé à *quelle* preuve *quand* (notamment une contrepartie ou un ADMIN téléchargeant les pièces adverses) est un événement de non-répudiation. `EvidenceService.deposit` écrit un audit atomique (propagation MANDATORY) ; `download` n'en écrit aucun. Hors intent capturé de la Story 1.4 (dont l'AC ne mentionne que l'accès sécurisé, et dont l'exigence d'audit epic est libellée « chaque **dépôt** génère une entrée d'audit ») — d'où le report plutôt qu'un ajout. Correctif = écrire un audit de consultation (action `EVIDENCE_DOWNLOADED`, id de pièce, acteur, rôle) via le writer d'audit unique. Décision de niveau produit + design : écrire un audit depuis une transaction `readOnly` change la sémantique de la lecture ; à trancher avant qu'une exigence de traçabilité de consultation ne devienne contractuelle (Epic 2 litige).
  status: RÉSOLU 2026-07-16 (Story 5.1 durcissement) — corrigé mais non marqué par la boucle. Bundle livré : plafond files[] (PlatformLimits), listes bornées (preuves + audit), audit EVIDENCE_DOWNLOADED, mapping EvidenceStorageException→enveloppe JSON, withdrawnAt/withdrawnBy au DTO, course loadSeq côté store. Build 110/110 vert.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-4-telecharger-une-preuve-en-toute-securite.md`
  summary: Les échecs du stockage objet autres que « objet absent » (MinIO indisponible, timeout, `S3Exception`, 403 de politique de bucket) ne sont pas mappés et produisent une réponse 500 whitelabel hors de l'enveloppe JSON de la plateforme.
  evidence: `EvidenceService.download` ne traduit que `EvidenceNotFoundException` (→ 404). Tout autre `S3Exception`/`SdkClientException` remonte non capturé ; `GlobalExceptionHandler` n'ayant pas de handler catch-all, le client reçoit le corps d'erreur Spring par défaut au lieu du contrat `{timestamp,status,error,message}` — incohérence de contrat d'erreur (et fuite potentielle de détails internes) sur le seul endpoint de lecture qui parle au stockage externe. Pré-existant et transverse : `deposit` appelle aussi `storage.store` sans mapping de ces échecs. Correctif = mapper les échecs stockage/infra vers un 5xx propre (ex. 502/503) dans `GlobalExceptionHandler` ou via une exception applicative dédiée, à décider de concert pour dépôt et téléchargement (contrat d'erreur de niveau plateforme).
  status: RÉSOLU 2026-07-16 (Story 5.1 durcissement) — corrigé mais non marqué par la boucle. Bundle livré : plafond files[] (PlatformLimits), listes bornées (preuves + audit), audit EVIDENCE_DOWNLOADED, mapping EvidenceStorageException→enveloppe JSON, withdrawnAt/withdrawnBy au DTO, course loadSeq côté store. Build 110/110 vert.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-1-ouvrir-un-litige-avec-preuve-obligatoire-atomique.md`
  summary: Un stranger authentifié qui sonde `POST /{id}/dispute` (comme tout endpoint escrow) distingue « existe mais non partie prenante » (`403`) de « inconnu » (`404`) — oracle d'existence de transaction (IDOR léger).
  status: ACCEPTÉ — RISQUE POC (décision Oscard, 2026-07-16) — non propre à 2.1 : c'est la sémantique de TOUS les endpoints escrow (`findById`→404 puis `resolveRole`→403). Le 403 honnête est retenu ; flipper toute la plateforme en 404 (masquage d'existence) n'en vaut pas le coût pour le POC (changement de contrat d'API + réécriture des tests asservissant 403). Risque explicitement accepté, dans l'esprit de NFR-6 ; conservé au ledger pour un durcissement prod.
  evidence: `TransactionAccess.resolveRole` lève `ForbiddenException` (403) avant toute autre vérification pour un non-partie, tandis qu'un id inconnu donne `404`. La Story 2.1 étend ce pattern à la surface litige et le test `openDisputeNonPartyIsForbidden` le fige comme comportement voulu. Pré-existant et transverse à toute la plateforme (`getDetail`, `deposit`, `download` font pareil) — d'où le report plutôt qu'un correctif ponctuel qui divergerait du reste. Correctif = renvoyer `404` uniformément pour inexistant ET non-partie, décidé au niveau plateforme (contrat d'autorisation commun), pas endpoint par endpoint. Sévérité faible (nécessite une session valide + énumération d'ids), à trancher comme posture d'API globale.
  resolution: RÉSOLU 2026-07-26 (Story 1.10, NFR-P9) — 404 uniforme décidé au niveau plateforme comme le prescrivait l'évidence ci-dessus : `NOT_A_PARTY` retiré de l'enum (pas seulement cessé d'être émis — le `code` était l'oracle autant que le statut), fabriques uniques `ApiExceptions.transactionNotFound()` / `evidenceNotFound()` sans identifiant dans le message, `TransactionAccess` (seuls sites de jet, canaux JWT et HMAC) bascule en 404. Prouvé par `AntiEnumerationIntegrationTest` : les deux réponses sont comparées **en entier** (statut + corps + en-têtes, horodatage normalisé) sur les 7 endpoints utilisateur, sur le canal partenaire signé et au niveau pièce. Exception documentée et testée : la garde « pièce à soi » reste un 403 honnête (l'appelant est partie et voit déjà la pièce par `list()`).

- source_spec: `_bmad-output/implementation-artifacts/spec-2-1-ouvrir-un-litige-avec-preuve-obligatoire-atomique.md`
  summary: Une ouverture de litige qui échoue au **stade dépôt** (fichier invalide `400`, ou stockage indisponible `500`) ne laisse **aucune** trace d'audit durable, alors qu'une transition illégale/non autorisée écrit un audit `REJECTED` (REQUIRES_NEW).
  status: RÉSOLU 2026-07-16 — `EscrowService.openDispute` enveloppe l'appel `evidenceService.deposit` : tout échec au stade dépôt écrit un `auditService.recordFailure` (REQUIRES_NEW, durable) avant de rethrow — même piste d'audit `REJECTED` qu'une transition illégale. Tests `EscrowDisputeServiceTest` mis à jour (fichier invalide + panne stockage asservissent désormais 1 audit `REJECTED`). 74/74 verts.
  evidence: Dans `EscrowService.openDispute`, le `recordSuccess(OPEN_DISPUTE)` de la transition est écrit en propagation MANDATORY puis annulé avec toute la transaction quand `evidenceService.deposit(...)` lève — si bien qu'une tentative d'ouverture ayant franchi la garde d'état mais échouée sur la preuve ne laisse rien dans `audit_logs`. Asymétrie avec le chemin `TransitionException` (audit `REJECTED` persistant). Non exigé par les AC de la Story 2.1 (l'AC de rollback n'impose que « pas de transition, pas de ligne evidence ») et cohérent avec le dépôt simple d'Epic 1 (un `deposit` échoué n'audite pas non plus) — d'où le report. Correctif éventuel = entourer l'appel `deposit` d'un `try/catch` qui écrit un `recordFailure` (REQUIRES_NEW) avant de relancer, à décider de concert avec une politique d'audit des échecs transverse (éviter d'auditer en bruit chaque 400 client). Sévérité moyenne : complétude de la piste d'audit dans un domaine où la non-répudiation des tentatives compte.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-1-ouvrir-un-litige-avec-preuve-obligatoire-atomique.md`
  summary: La surface HTTP de `POST /{id}/dispute` n'a aucun test de couche web — les codes de statut (200 / 400 / 403 / 404 / 409), le binding multipart (part `files` requis, `comment` requis, `clientCapturedAt` optionnel) et la sérialisation de `DisputeOpenedDto` ne sont vérifiés qu'indirectement via des tests service (`@DataJpaTest` appelant `escrowService.openDispute`).
  status: RÉSOLU 2026-07-16 — nouveau `EscrowControllerDisputeTest` (MockMvc `standaloneSetup` + `GlobalExceptionHandler` + resolver du principal, sans DB ni sécurité) : 7 tests couvrant 200 (avec sérialisation `DisputeOpenedDto`), `files`/`comment` manquants → 400, `clientCapturedAt` optionnel, et le mapping des exceptions service → 409/404/403. 74/74 verts.
  evidence: `EscrowDisputeServiceTest` couvre toute la matrice I/O au niveau service et prouve les *types* d'exception levés (`BadRequestException`, `ForbiddenException`, `TransitionException` ILLEGAL/UNAUTHORIZED, `NotFoundException`), mais rien ne prouve de bout en bout que `GlobalExceptionHandler` mappe bien ces exceptions vers 400/403/404/409 sur *cet* endpoint, ni que la part `files` manquante donne un 400 propre, ni que `consumes=MULTIPART_FORM_DATA` et la réponse `200 DisputeOpenedDto` se comportent comme spécifié. Le mapping est centralisé et déjà exercé par les autres endpoints (`applyEvent`, dépôt de preuve), donc le risque marginal est faible — mais le projet n'a aucun test de tranche web (`@WebMvcTest`/`MockMvc`) et la story est libellée en codes HTTP. Gap réel sur une surface HTTP nouvelle (premier POST multipart renvoyant un DTO composite en 200). Correctif = un `@WebMvcTest`/`MockMvc` (ou test d'intégration full-stack) sur `/dispute`, idéalement de concert avec une décision de convention de test de couche web pour toute la plateforme. Sévérité faible.

- source_spec: `spec-2-3-retirer-sa-propre-piece-avec-plancher-de-preuve.md`
  summary: `EvidenceDto` n'expose ni `withdrawn_at` ni `withdrawn_by_user_id`, donc aucune réponse API (retrait, liste, dépôt) ne révèle qui a retiré une pièce ni quand.
  evidence: La ligne DB (`ck_evidence_withdrawal`) et l'audit `EVIDENCE_WITHDRAWN` capturent l'attribution, mais le DTO de réponse (hérité de la Story 1.3) s'arrête à `createdAt`. Les AC de la Story 2.3 n'exigent que les champs DB renseignés + l'entrée d'audit, non leur exposition en réponse ; à trancher pour la Story 2.4 (fil contradictoire PWA) qui affiche les pièces retirées.
  status: RÉSOLU 2026-07-16 (Story 5.1 durcissement) — corrigé mais non marqué par la boucle. Bundle livré : plafond files[] (PlatformLimits), listes bornées (preuves + audit), audit EVIDENCE_DOWNLOADED, mapping EvidenceStorageException→enveloppe JSON, withdrawnAt/withdrawnBy au DTO, course loadSeq côté store. Build 110/110 vert.

- source_spec: `spec-2-4-interface-pwa-ouvrir-un-litige-fil-contradictoire.md`
  summary: Le retrait de preuve met à jour `items` en place sans participer au jeton de séquence `loadSeq` du store evidence — un `loadEvidence` en vol (montage / `escrow:sync`) résolu après le commit du retrait peut réafficher brièvement la pièce en `ACTIVE`.
  evidence: `evidenceStore.withdrawEvidence` fait `items = items.map(i => i.id === dto.id ? dto : i)` (mutation ciblée voulue par la spec) alors que `loadEvidence` remplace tout `items` sous garde `loadSeq`. Un GET liste émis avant le commit serveur du retrait et arrivé après écrase l'état retiré par le snapshot `ACTIVE` pré-retrait. Fenêtre très étroite (nécessite un `escrow:sync` concurrent d'un clic « Withdraw »), auto-corrigée au prochain chargement, et le serveur reste cohérent (retrait committé). Même classe de course optimiste-vs-rechargement que le dépôt d'Epic 1.5. Correctif = faire participer le retrait à `loadSeq` ou refetch ciblé, à décider de concert avec une politique de cohérence optimiste du store evidence (dépôt + retrait). Sévérité faible.
  status: RÉSOLU 2026-07-16 (Story 5.1 durcissement) — corrigé mais non marqué par la boucle. Bundle livré : plafond files[] (PlatformLimits), listes bornées (preuves + audit), audit EVIDENCE_DOWNLOADED, mapping EvidenceStorageException→enveloppe JSON, withdrawnAt/withdrawnBy au DTO, course loadSeq côté store. Build 110/110 vert.

- source_spec: `spec-5-1-durcissement-contrat-api-preuves.md`
  summary: Les listes désormais bornées (`GET .../evidence` et le trail d'audit de `getDetail`) tronquent silencieusement le **bout le plus récent** — `ORDER BY ... ASC LIMIT 500` garde les 500 plus anciennes sans aucun signal (header, curseur, flag), et le nouvel audit `EVIDENCE_DOWNLOADED` gonfle le trail vers le plafond à chaque téléchargement.
  evidence: Le durcissement de la Story 5.1 a introduit une borne dure `PlatformLimits.MAX_LIST_RESULTS = 500` (choix POC documenté, cohérent sur les deux endpoints) qui satisfait l'AC « aucune liste non bornée ». Mais au-delà de 500 lignes, ce sont les événements les plus récents qui disparaissent de la réponse, ce qui est contre-intuitif pour un trail de conformité, et l'écriture d'un audit par download rend le plafond plus atteignable sur un litige actif. Pour un POC à faible volume par transaction, non déclenchable en pratique ; mais avant l'Epic 4 (rejeu offline massif) le correctif propre = pagination/curseur navigable (ou, pour le trail d'audit, `DESC LIMIT 500` puis ré-inversion pour l'affichage) + un indicateur de troncature. Sévérité faible-moyenne, durcissement de niveau contrat d'API.
  status: RÉSOLU 2026-07-16 (Story 5.2) — corrigé mais non marqué par la boucle. A: listes bornées conservent désormais les N plus RÉCENTES (sélection created_at DESC puis restitution) ; B: download gère size_bytes NULL (Content-Length omis) ; C: flux S3 fermé via try/catch closeQuietly si le build de réponse échoue. Build 117/117.

- source_spec: `spec-5-1-durcissement-contrat-api-preuves.md`
  summary: `EvidenceService.download` déréférence `long sizeBytes = evidence.getSizeBytes()` sur une colonne `size_bytes BIGINT` **nullable** (entité `Long`) — un unboxing NPE si le champ est nul, produisant un 500 whitelabel pour un ayant droit et n'écrivant aucun audit de download.
  evidence: Pré-existant (chemin de lecture hérité de la Story 1.4), simplement re-exposé par la revue de la Story 5.1 : la NPE survient avant `storage.load` et avant le nouvel audit. En pratique tout dépôt renseigne `size_bytes`, donc non déclenchable sur des données produites par l'application ; mais le schéma l'autorise (aucun `NOT NULL`) et une ligne importée/altérée hors application le déclencherait. Correctif = soit rendre `size_bytes` `NOT NULL` en migration (de concert avec les CHECK de V3), soit garder l'unboxing défensif (`getSizeBytes() == null ? ... : ...`). Sévérité faible.
  status: RÉSOLU 2026-07-16 (Story 5.2) — corrigé mais non marqué par la boucle. A: listes bornées conservent désormais les N plus RÉCENTES (sélection created_at DESC puis restitution) ; B: download gère size_bytes NULL (Content-Length omis) ; C: flux S3 fermé via try/catch closeQuietly si le build de réponse échoue. Build 117/117.

- source_spec: `spec-5-1-durcissement-contrat-api-preuves.md`
  summary: `EvidenceService.download` ouvre un flux S3 vivant (`storage.load`) puis, la transaction étant devenue inscriptible (audit `EVIDENCE_DOWNLOADED`), un échec du **COMMIT** JPA après le retour de la méthode ne ferme jamais le flux — ni le `catch` du service (qui ne garde que l'appel `recordEvidenceDownloaded`) ni le `catch` du contrôleur (qui ne garde que la construction de la réponse) ne s'exécutent, laissant fuir la connexion/bail de pool de stockage.
  evidence: Introduit/élargi par la Story 5.1 : le passage de `@Transactional(readOnly=true)` à `@Transactional` inscriptible ajoute un flush/commit qui peut lever (drop de connexion au flush) **après** que `download()` a renvoyé son `EvidenceDownload` avec le flux déjà ouvert, mais **avant** que le contrôleur ne le streame. Avec l'ancien download `readOnly` la fenêtre était quasi vide (aucun INSERT à flusher) ; l'écriture d'audit l'élargit. Fenêtre étroite et infra-dépendante (le COMMIT doit échouer, flux déjà ouvert), auto-limitée (le pool récupère le bail par timeout/éviction), non déclenchable au volume POC. Non corrigeable trivialement dans la méthode service (le commit survient dans le proxy après le retour) : le correctif propre relève de la frontière transaction/contrôleur — p.ex. déférer l'ouverture du flux hors de la transaction d'audit, ou fermer `content` sur toute défaillance de livraison via un wrapper de cycle de vie côté contrôleur/`EvidenceDownload`. Sévérité faible.
  status: RÉSOLU 2026-07-16 (Story 5.2) — corrigé mais non marqué par la boucle. A: listes bornées conservent désormais les N plus RÉCENTES (sélection created_at DESC puis restitution) ; B: download gère size_bytes NULL (Content-Length omis) ; C: flux S3 fermé via try/catch closeQuietly si le build de réponse échoue. Build 117/117.

- source_spec: `spec-5-2-corriger-reports-durcissement.md`
  summary: `EvidenceController.download` appelle `MediaType.parseMediaType(d.contentType())` sur un `mime_type` **nullable** (colonne sans `NOT NULL`) — symétrique de la NPE `size_bytes` corrigée en Story 5.2 (report B), mais sur `mime_type` : une pièce à `mime_type` nul lève `IllegalArgumentException`, capturée par le `catch(RuntimeException)` du contrôleur (flux fermé) puis remontée en page whitelabel `500` pour un ayant droit.
  evidence: Pré-existant (chemin de lecture hérité de la Story 1.4), hors périmètre de la Story 5.2 (qui ne traitait que `size_bytes`/report B). En pratique tout dépôt renseigne `mime_type` via le validateur (jamais nul sur données applicatives), donc non déclenchable en pratique ; mais le schéma l'autorise et une ligne importée/altérée hors application le déclencherait. Correctif = soit garder `d.contentType() == null ? MediaType.APPLICATION_OCTET_STREAM : parseMediaType(...)` au contrôleur, soit rendre `mime_type` `NOT NULL` en migration. Sévérité faible. À traiter de concert avec le report `size_bytes NOT NULL` s'il est un jour décidé.
  status: RÉSOLU 2026-07-16 (fix manuel) — download replie sur application/octet-stream quand mime_type est NULL (symétrique au fix size_bytes), plus de parseMediaType(null). Test EvidenceControllerDownloadTest ajouté (4/4).


- source_spec: `spec-3-1-provisionner-les-cles-hmac-entrantes-le-magasin-de-nonces.md`
  summary: La clé HMAC entrante (`partner_hmac_keys.secret_key`) est admise sans garde de longueur/entropie minimale et exposée par un getter simple sans `@JsonIgnore` — un secret trivial (les tests persistent `"S"`) ou une sérialisation accidentelle de l'entité fragiliserait l'authentification partenaire.
  evidence: La Story 3.1 pose le socle de persistance sans chemin de provisioning applicatif (les clés sont insérées via le repository) ; le chiffrement au repos est un choix POC explicitement sanctionné par la spec (aligné sur `webhook_subscriptions.secret_key` en clair), mais aucune garde n'empêche un secret 1-octet et l'entité n'a pas de garde de sérialisation. Non déclenchable tant qu'aucun contrôleur ne renvoie/log l'entité (aucun en 3.1). Correctif = à poser quand l'outillage de provisioning/admin arrivera : `CHECK (char_length(secret_key) >= 32)` ou validation applicative + `@JsonIgnore` sur `getSecretKey()` (idéalement appliqué symétriquement au secret sortant). Sévérité faible.
  status: RÉSOLU 2026-07-16 (Story 3.3) — migration `V5` : `CHECK (octet_length(secret_key) >= 32)` (garde de longueur à l'admission) ; `getSecretKey()` porte `@JsonProperty(access = WRITE_ONLY)` (jamais sérialisé en sortie, bindable en entrée pour 3.2). Tests `weakSecretRejected` + `secretLengthBoundaryEnforced` + `secretNotSerialized`.

- source_spec: `spec-3-1-provisionner-les-cles-hmac-entrantes-le-magasin-de-nonces.md`
  summary: La FK `partner_key_nonces.key_id → partner_hmac_keys(key_id) ON DELETE CASCADE` fait qu'un hard-delete d'une clé efface tout son historique de nonces ; comme `key_id` n'est UNIQUE que parmi les lignes vivantes, re-provisionner la même `key_id` après suppression repart d'un magasin de nonces vide et réouvre la fenêtre de rejeu.
  evidence: La révocation est modélisée en soft-delete (`active=false` ; le repository ne résout que les clés actives), donc le hard-delete + réutilisation de `key_id` n'est pas un flux supporté, et la fenêtre de rejeu ne compte que ≤ rétention (±5 min). Le risque exige donc une opération non supportée dans une fenêtre étroite. Correctif = documenter/imposer le cycle de vie clé en *soft-delete only* (jamais `DELETE`), ou basculer la FK en `RESTRICT`/`NO ACTION` pour que l'historique de nonces ne soit jamais détruit en effet de bord (à décider de concert avec l'outillage d'admin). Sévérité faible.
  status: RÉSOLU 2026-07-16 (Story 3.3) — migration `V5` bascule la FK `partner_key_nonces.key_id → partner_hmac_keys(key_id)` de `ON DELETE CASCADE` en `ON DELETE RESTRICT` : un hard-delete d'une clé ayant des nonces est bloqué par la DB (historique jamais effacé en effet de bord). Tests `keyDeleteRestrictedByNonceHistory` (bloqué) + `nonceFreeKeyIsDeletable` (contre-preuve). `key_id` reste `UNIQUE`.

- source_spec: `spec-3-3-durcir-stockage-cles-hmac-partenaire.md`
  summary: Avec la FK nonces→clés désormais en `ON DELETE RESTRICT`, le hard-delete (GC) d'une clé partenaire retirée exige de purger d'abord tout son historique de nonces ; entre deux purges bornées, la queue récente subsiste et rend la clé non supprimable à la demande, sans procédure/outil « purge-puis-delete » documenté.
  evidence: Voulu : la révocation passe par le soft-delete (`active=false`), seul flux supporté, et RESTRICT ne bloque que la destruction d'historique — pas un défaut de code. La GC réelle d'un partenaire (suppression physique) relèvera de l'outillage d'admin (hors périmètre 3.3 / à cadrer avec 3.2+). Correctif futur = fournir un chemin explicite `deleteByKeyId(nonces)` puis `delete(key)`, ou un helper d'archivage. Sévérité faible.
  status: ACCEPTÉ — PAR CONCEPTION (2026-07-16) — conséquence VOULUE du fix 3.3 : la FK ON DELETE RESTRICT bloque délibérément le hard-delete d'une clé ayant un historique de nonces (préserve l'anti-rejeu). La rotation/GC de clés est un besoin futur (désactivation via is_active, purge des nonces expirés d'abord) — hors POC. Documenté, non bloquant.

- source_spec: `spec-3-2-deposer-une-preuve-partenaire-via-api-signee.md`
  summary: Avec l'endpoint partenaire désormais live (`POST /api/v1/partner/escrow/{id}/evidence`), la table `partner_key_nonces` croît sans borne : `deleteBySeenAtBefore` existe (Story 3.1) mais n'est jamais planifiée (`@Scheduled` explicitement hors périmètre par la spec 3.2), donc la protection anti-rejeu reste correcte indéfiniment mais la sonde `existsByKeyIdAndNonce` et l'index unique se dégradent à mesure que la table grossit sur le chemin chaud de dépôt.
  evidence: Décision POC sanctionnée par la clause « Never » de la spec 3.2 (pas de job de purge). Réel opérationnellement : un partenaire actif accumule des millions de lignes de nonces, augmentant la latence insert/probe et le stockage sans contrôle de rétention. Correctif futur = ordonnanceur bornant la rétention à ≥ fenêtre timestamp (≥ 5 min) appelant `deleteBySeenAtBefore(now - rétention)`. Sévérité faible ; à cadrer avec le durcissement opérationnel post-POC.
  status: RÉSOLU 2026-07-17 (Story 3.4) — `SchedulingConfig` (`@EnableScheduling`) + `PartnerNoncePurger` planifié (`scheduledPurge()` `@Scheduled @Transactional`) appelant `deleteBySeenAtBefore(now - max(rétention, tolérance))` ; rétention par défaut 900 s ≥ 300 s de tolérance, le `max(...)` interdisant qu'une rétention < tolérance rouvre la fenêtre de rejeu. Test `PartnerNoncePurgerIntegrationTest` (Testcontainers) : nonce expiré purgé, nonce récent conservé, plancher de tolérance respecté.

- source_spec: `spec-3-2-deposer-une-preuve-partenaire-via-api-signee.md`
  summary: La route signée est ouverte par un matcher `permitAll` sur tout le sous-arbre `/api/v1/partner/**` (`SecurityConfig`), plus large que l'unique endpoint réellement porté par la signature — tout futur contrôleur monté sous `/api/v1/partner/` serait silencieusement non authentifié, sans garde compensatoire ni test de garde-fou.
  evidence: Aujourd'hui correct (le commentaire note « aucun autre endpoint ouvert par ce matcher ») et le seul contrôleur mappé est `POST /api/v1/partner/escrow/{id}/evidence` dont l'auth = signature seule. Mais Epic 3+ peut ajouter d'autres routes partenaire ; le wildcard les exposerait par défaut. Correctif = épingler le `permitAll` au couple méthode+chemin exact (ou ajouter un test asservissant qu'aucune autre route partenaire n'est ouverte). Différé comme durcissement de configuration de sécurité ; sévérité faible-moyenne (risque futur, pas de défaut courant).
  status: RÉSOLU 2026-07-17 (Story 3.4) — `SecurityConfig` épingle le `permitAll` au couple `HttpMethod.POST` + `/api/v1/partner/escrow/*/evidence` ; toute autre route `/api/v1/partner/**` retombe sur `anyRequest().authenticated()`. Test `PartnerSecurityMatcherTest` (contexte complet + Testcontainers) : GET sur le chemin exact → 403 (scope méthode), POST sur un chemin frère → 403 (scope chemin), endpoint réel sans JWT → 400 (traverse la sécurité).

- source_spec: `spec-3-2-deposer-une-preuve-partenaire-via-api-signee.md`
  summary: Le `catch (DataIntegrityViolationException)` autour de l'INSERT du nonce (`PartnerEvidenceService.deposit`) est un catch-all qui traite **toute** violation d'intégrité comme un rejeu de nonce (`401`), et aucune garde de longueur n'est appliquée en amont sur les en-têtes (`X-Escrow-Nonce > 255` déclenche l'ingest complet puis un rollback rendu en `401` au lieu d'un `400`).
  evidence: Fail-safe aujourd'hui (la transaction rollback, aucune corruption) et le commentaire justifie que la génération IDENTITY force l'INSERT immédiat, si bien que seule `uq_partner_key_nonces_key_nonce` peut lever à ce point. Mais le raisonnement dépend de l'ordre de flush et de l'absence d'autre contrainte pendante : une violation FK/autre au flush serait masquée en `401` de rejeu, très difficile à diagnostiquer ; et un nonce sur-long fait tourner tout l'ingest avant de rendre un code trompeur. Correctif = restreindre le catch (inspection du nom de contrainte) et/ou valider en amont la longueur des en-têtes avant tout travail coûteux. Différé : l'extraction fiable du nom de contrainte est spécifique au SGBD ; sévérité faible.
  status: RÉSOLU 2026-07-17 (Story 3.4) — `isNonceReplay` parcourt toute la chaîne de causes pour un `ConstraintViolationException` Hibernate nommant `uq_partner_key_nonces_key_nonce` (insensible à la casse) : seul ce conflit → rejeu 401, toute autre violation remonte (500). Garde de longueur du nonce (>255 → 400) en tête de `deposit`, avant vérif signature/ingest. Prouvé sur vraie base par le test de concurrence (le vrai conflit de nonce Postgres est reconnu en 401) + tests unitaires distinguant les deux cas et le nonce sur-long.

- source_spec: `spec-3-2-deposer-une-preuve-partenaire-via-api-signee.md`
  summary: Aucun test d'intégration sur vraie base ne persiste une ligne partenaire réelle (`uploader_type=CARRIER_PARTNER`, `partner_company_id` renseigné, `uploaded_by_user_id=null`, audit acteur/rôle null) contre le CHECK `ck_evidence_attribution` (V3) — l'invariant le plus porteur de la story (ligne partenaire valide + audit `EVIDENCE_ADDED` committés ensemble) n'est prouvé que par des mocks.
  evidence: `PartnerEvidenceServiceTest` mocke `EvidenceService` et `PartnerEvidenceControllerTest` fabrique des `EvidenceFile` à la main ; le chemin `depositAsPartner`→`ingest` n'est jamais exécuté contre un Postgres réel avec V3 appliquée. La clause « Block If » de la spec dépend précisément de la compatibilité entre l'attribution partenaire et ce CHECK. Un `@DataJpaTest`/Testcontainer déposant une pièce partenaire de bout en bout fermerait le trou (l'infra existe déjà — `PartnerKeyStoreTest` tourne sur Postgres réel). Différé : test d'intégration non trivial ; sévérité moyenne (critère d'acceptation asservi seulement par mocks).
  status: RÉSOLU 2026-07-17 (Story 3.4) — `PartnerEvidenceDepositIntegrationTest` (`@DataJpaTest` + Testcontainers Postgres, Flyway V1→V5, fake storage) dépose une pièce partenaire signée de bout en bout et relit la ligne `evidence_files` réelle : `uploader_type=CARRIER_PARTNER`, `partner_company_id` renseigné, `uploaded_by_user_id=null`, `status=ACTIVE`, nonce consommé, audit `EVIDENCE_ADDED` `action_by` null — l'invariant `ck_evidence_attribution` tient sur vraie base.

- source_spec: `spec-3-2-deposer-une-preuve-partenaire-via-api-signee.md`
  summary: L'atomicité anti-rejeu n'est asservie que par mock : `nonceInsertRaceIsUnauthorized` stubbe `save(...)` pour lever `DataIntegrityViolationException` (preuve du catch), mais rien ne prouve que `uq_partner_key_nonces_key_nonce` arbitre réellement deux INSERT concurrents `(key_id, nonce)` identiques, ni qu'un dépôt annulé laisse le nonce non consommé (garantie de retry avec le même nonce).
  evidence: La revendication de concurrence centrale de la story n'a aucun test touchant la base. Un test d'intégration concurrent (deux transactions, même nonce) confirmerait l'arbitrage réel de la contrainte unique et la non-consommation du nonce au rollback. Infra Testcontainer déjà disponible (cf. `EvidenceWithdrawConcurrencyTest`). Différé : test de concurrence non trivial ; sévérité moyenne.
  status: RÉSOLU 2026-07-17 (Story 3.4) — `PartnerNonceConcurrencyIntegrationTest` (Testcontainers, `@Transactional(NOT_SUPPORTED)` pour un vrai commit par appel) : deux dépôts concurrents au même `(key_id, nonce)` → exactement un réussit, l'autre rejeté rejeu (`UnauthorizedException`) via la contrainte `uq_partner_key_nonces_key_nonce` ; la transaction perdante rollback (dépôt annulé, nonce non consommé) — en base exactement 1 ligne evidence + 1 nonce.

- source_spec: `spec-3-2-deposer-une-preuve-partenaire-via-api-signee.md`
  summary: `TransactionAccess.requireCompanyParticipant` charge jusqu'à deux agrégats `User` complets (`users.findById(buyerId)` puis `sellerId`) juste pour comparer un id de société, à chaque dépôt partenaire, à l'intérieur de la transaction d'écriture qui tient déjà le verrou pessimiste sur la transaction.
  evidence: `belongsToCompany` matérialise l'entité `User` (et touche `Company` en lazy) pour ne lire qu'un `Company.id`. Une requête de projection (`existsByIdInAndCompanyId`, ou une jointure sur la transaction) serait moins coûteuse et raccourcirait la durée de détention du verrou. Différé comme optimisation ; sévérité faible (le triage de cette passe a ajouté un test unitaire direct du garde — `TransactionAccessTest` — mais pas l'optimisation de requête).
  status: ACCEPTÉ — MINEUR POC (2026-07-17) — micro-inefficacité (2 chargements User) ; non bloquant.

- source_spec: `spec-3-2-deposer-une-preuve-partenaire-via-api-signee.md`
  summary: Le message d'échec d'authentification `"Invalid partner credentials"` est déclaré en double (`PartnerEvidenceService.AUTH_FAILED` et une constante privée de `PartnerSignatureVerifier`) alors que l'uniformité du message est une propriété de sécurité voulue (anti-énumération) — deux copies indépendantes invitent une dérive future où l'une change et la propriété anti-oracle casse silencieusement.
  evidence: Les deux constantes sont aujourd'hui identiques ; le risque est la maintenance (un seul point devrait porter le message uniforme). Correctif = extraire une constante partagée unique. Différé : churn de code de production pour un gain cosmétique ; sévérité faible.
  status: ACCEPTÉ — MINEUR POC (2026-07-17) — duplication de littéral, cosmétique ; non bloquant.


- source_spec: `_bmad-output/implementation-artifacts/spec-4-2-ouvrir-un-litige-deposer-des-preuves-hors-ligne.md`
  summary: Le plafond serveur de 20 fichiers par dépôt (`PlatformLimits.MAX_FILES_PER_DEPOSIT`) n'a aucun miroir côté front ; hors-ligne, aucun serveur ne dit non au moment du dépôt.
  evidence: `utils/evidence.js:47-60` valide taille et MIME mais jamais le nombre ; `OpenDisputeForm.vue` n'exige que `length > 0`. Un litige hors-ligne à 21 photos est mis en file sans broncher, puis rejeté en 400 au rejeu. En ligne c'est un retour immédiat et corrigeable ; en file, l'entrée attend le traitement des rejets permanents de la Story 4.3.
  status: OUVERT — miroir client du plafond serveur ; Epic 9 (offline complet) ou la story UI de dépôt.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-2-ouvrir-un-litige-deposer-des-preuves-hors-ligne.md`
  summary: `navigator.onLine` est le seul déclencheur de la mise en file : un POST parti en ligne qui échoue faute de réseau n'est jamais mis en file, et ses binaires sont perdus.
  evidence: `escrow.js` branche sur `offlineQueue.isOnline` — motif identique à `createNewTransaction` et `sendTransactionEvent` depuis 4.1, donc pré-existant. Mais sur portail captif ou une barre de réseau (le terrain visé par l'epic), `navigator.onLine` vaut `true`, le POST expire et le formulaire dit « please try again » sans que rien ne soit conservé. La Story 4.2 rend cette perte coûteuse (des photos, plus un événement JSON rejouable). Un repli « échec réseau ⇒ mise en file » demande de trancher le risque de double soumission — décision humaine.
  status: OUVERT — Epic 9 (offline complet) : `navigator.onLine` ment sur portail captif.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-2-ouvrir-un-litige-deposer-des-preuves-hors-ligne.md`
  summary: Un rechargement hors-ligne du détail d'une transaction efface tout signe du litige en attente : l'utilisateur voit un panneau d'erreur.
  evidence: `TransactionDetailView.vue:onMounted` appelle `load()` sans garde `isOnline` ; hors-ligne `loadTransactionDetail` échoue, pose `escrowStore.error`, et le bloc `v-else-if="transaction"` — donc le bandeau `_queuedDispute` — n'est jamais monté. L'entrée reste en base et rejouable (prouvé au niveau du store), mais l'affichage optimiste est **scopé à la session**. Pré-existant (tout détail hors-ligne se comporte ainsi) ; le vrai correctif exige un cache du détail ou l'écran de récupération de la Story 4.5, pas un garde de plus.
  status: OUVERT — Epic 9 (offline complet).

- source_spec: `_bmad-output/implementation-artifacts/spec-4-2-ouvrir-un-litige-deposer-des-preuves-hors-ligne.md`
  summary: Aucun test de composant dans le repo alors que `@vue/test-utils` est installé depuis 4.1 : les trois comportements UI de cette story ne sont vérifiés qu'à l'œil.
  evidence: Le garde `isOnline` de `onDisputeOpened` n'est pas cosmétique (sans lui, un rechargement hors-ligne remplace le détail optimiste par une erreur), et ni lui, ni le bandeau `_queuedDispute`, ni la pastille « Pending sync » de `TransactionCard.vue` n'ont de test. Le harnais qui les couvrirait a déjà été payé par la Story 4.1.
  status: OUVERT (périmètre CORRIGÉ 2026-07-28) — **la prémisse du balayage était fausse et l'entrée disait vrai à sa date, plus aujourd'hui.** Vérifié fichier par fichier : des tests de composant existent depuis la Story 4.4 (`src/components/__tests__/SyncFailureNotice.spec.js`, `.recovery.spec.js`, commits cf3d13c et cc95980), ils appellent bien `mount()` de `@vue/test-utils`, et `vitest.config.js` porte le plugin `vue()` — mis là pour eux, comme son propre commentaire l'explique. Le harnais et la convention EXISTENT ; « aucun test de composant » était vrai en Story 4.2 et a été démenti deux stories plus tard sans que le ledger soit refermé. Le balayage du 2026-07-27 a relevé la priorité de cette entrée au lieu de vérifier son contenu — le motif exact que ce même balayage dénonçait. Rien à livrer côté outillage pour l'Epic 2 : `mount()` fonctionne aujourd'hui.
  Ce qui RESTE ouvert, et qui est autre chose : la **couverture**, 3 composants sur 14 (`SyncFailureNotice` ×2, `AuthView`, `RecoveryView`). Les trois comportements nommés par cette entrée en font partie — pastille « Pending sync » de `TransactionCard.vue`, bandeau `_queuedDispute` et garde `isOnline` de `TransactionDetailView.vue`, tous trois toujours sans test. Reroutage : ce sont des composants du parcours hors-ligne POC → **Epic 9**, ou la story qui les réécrit. La règle « toute story qui livre ou réécrit un composant apporte son test de composant » est inscrite dans `project-context.md` (bundle QUALITÉ-CI, 2026-07-28) : c'est elle qui couvre 2-2, pas cette entrée.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-2-ouvrir-un-litige-deposer-des-preuves-hors-ligne.md`
  summary: Le dépôt de preuve **simple** (hors ouverture de litige) reste en ligne uniquement, et aucune story de l'Epic 4 ne le couvre.
  evidence: `stores/evidence.js#uploadEvidence` et `EvidenceDeposit.vue` sont intacts : hors-ligne, un utilisateur en `SHIPPED` qui dépose une pièce obtient « Upload failed. Please try again. ». Le corps de l'Epic 4 (« il rend le dépôt de preuves **et** l'ouverture de litige utilisables sur un réseau instable ou absent ») promet plus que ses stories : 4.2 ne cible que le chemin composite, 4.3/4.4/4.5 traitent réconciliation, notification et récupération. Trou de couverture au niveau de l'epic, à arbitrer.
  status: OUVERT — Epic 9 (offline complet).

- source_spec: `_bmad-output/implementation-artifacts/spec-4-3-reconcilier-la-file-a-la-synchronisation.md`
  summary: La file hors-ligne IndexedDB est globale et n'est clefée par aucun utilisateur : si B se connecte sur l'appareil après l'expiration de la session de A, les mutations en attente de A sont rejouées sous le JWT de B.
  evidence: `offlineQueue.idb.js:3-5` — `DB_NAME = 'escrow-offline'`, aucun index ni discriminant d'utilisateur ; `client.js:13-20` injecte le JWT courant à l'émission, jamais celui en vigueur à la mise en file. Résultat : soit `NOT_A_PARTY` → gel définitif des preuves de A, soit un succès silencieux si B est aussi partie à la transaction. Pré-existant (introduit avec la file en 4.1), mais la Story 4.3 s'appuie sur le raisonnement « une session expirée est transitoire car la même requête est valide après ré-authentification » — vrai seulement si c'est le *même* utilisateur qui se ré-authentifie.
  RÉSOLU 2026-07-26 (Story 1.9) : le scoping est passé dans la signature de la couche de persistance (`getAllForUser`/`clearForUser`, `ownsEntry`), et `hydrate()`/`flush()` ne voient plus que les entrées de l'utilisateur authentifié — pas d'index ni de `DB_VERSION = 2`, arbitrage documenté dans `offlineQueue.idb.js`.
  status: RÉSOLU 2026-07-26 (Story 1.9) — **vérifié contre le code au balayage 2026-07-27**, l'entrée était restée ouverte alors qu'une story ultérieure l'avait close : la file est désormais clefée par utilisateur (`getAllForUser(userId)` / `clearForUser`, `offlineQueue.idb.js`), une entrée sans `meta.userId` n'appartenant à personne. Un second utilisateur ne peut plus rejouer la file du premier.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-3-reconcilier-la-file-a-la-synchronisation.md`
  summary: `escrow:sync` peut être émis avant qu'aucun écouteur ne soit monté, l'affichage optimiste survit alors à sa réconciliation.
  evidence: `main.js:17` appelle `useOfflineQueueStore().init()` — qui enchaîne sur `flush()` — **avant** `app.mount('#app')`, alors que les écouteurs sont posés dans les `onMounted` de `DashboardView.vue:16-26` et `TransactionDetailView.vue:101-113`. Un `flush()` au démarrage dispatche donc potentiellement dans le vide. Pré-existant (le dispatch existait pour `syncedAny`), mais la Story 4.3 lui confie une responsabilité nouvelle : c'est désormais lui qui annule un affichage optimiste devenu mensonger.
  status: OUVERT — Epic 9 (offline complet).

- source_spec: `_bmad-output/implementation-artifacts/spec-4-3-reconcilier-la-file-a-la-synchronisation.md`
  summary: L'intercepteur HTTP ne traite pas le 403 nu comme une session expirée, alors que c'est le statut réellement émis par ce backend dans ce cas — l'utilisateur n'est jamais redirigé vers la ré-authentification.
  evidence: `client.js:27-32` ne purge la session et ne redirige vers `/auth` que sur `401`. Or `SecurityConfig.java:35-49` ne configure ni `httpBasic`, ni `formLogin`, ni `AuthenticationEntryPoint` : un JWT expiré, avalé par `JwtAuthFilter.java:54-57`, échoue sur `anyRequest().authenticated()` via `Http403ForbiddenEntryPoint` → **403**, confirmé par le commentaire de `PartnerSecurityMatcherTest.java:72`. La Story 4.3 classe désormais correctement ce 403 nu en transitoire (rien n'est perdu, la file attend), mais l'utilisateur n'est pas invité à se reconnecter : la file reste en attente sans explication. Le correctif tient en une condition dans l'intercepteur, mais il touche l'authentification globale de l'app — hors périmètre d'une story front de réconciliation.
  RÉSOLU 2026-07-26 (Story 1.9) : prédicat unique `isBareAuthFailure` partagé par `classifyReplayFailure` et l'intercepteur, qui émet `escrow:session-expired` (une seule fois, verrou levé par `beginSession`) ; `installSessionExpiryListener` purge et renvoie vers `auth?redirect=<cible>`.
  status: RÉSOLU 2026-07-26 (Story 1.9) — **vérifié contre le code au balayage 2026-07-27**, l'entrée était restée ouverte alors qu'une story ultérieure l'avait close : `api/client.js` émet `escrow:session-expired` sur tout 401/403 **nu** avec jeton présent, en laissant délibérément passer les 4xx **codés** (un 403 codé ne déconnecte pas).

- source_spec: `_bmad-output/implementation-artifacts/spec-4-4-notifier-l-utilisateur-du-resultat-de-synchronisation.md`
  summary: `logout()` ne vide ni la file hors-ligne ni son store IndexedDB, si bien que les entrées d'un utilisateur peuvent être rejouées — ou consultées — sous la session d'un autre sur un appareil partagé.
  evidence: Pré-existant (surfacé par la revue de 4.4, à rapprocher du report « file non clefée par utilisateur » de 4.3). `stores/auth.js#logout()` efface le token et l'utilisateur, jamais `queue` ni le store `escrow-offline` ; `DashboardView.vue:44` force un rechargement complet et `main.js:17` appelle `init()` inconditionnellement, ce qui réhydrate la file de l'utilisateur précédent. La Story 4.4 a masqué la fuite d'affichage (notice conditionnée à `auth.isAuthenticated`) mais **pas le rejeu** : `flush()` repartirait avec le JWT du nouvel utilisateur — le serveur rejetterait en `NOT_A_PARTY` (donc gel, pas de faille d'autorisation), mais l'entrée du premier utilisateur serait gelée à tort et deviendrait irrécupérable pour son propriétaire.
  RÉSOLU 2026-07-26 (Story 1.9) : `endSession({reason:'logout'})` vide la mémoire de la file et supprime d'IndexedDB les entrées du partant **et** les orphelines — jamais celles d'un autre utilisateur — puis purge `escrow-api-cache` ; le rejeu est en outre filtré par `ownsEntry` dans `flush()`.
  status: RÉSOLU 2026-07-26 (Story 1.9) — **vérifié contre le code au balayage 2026-07-27**, l'entrée était restée ouverte alors qu'une story ultérieure l'avait close : `stores/session.js` appelle `idb.clearForUser(userId)` et `useOfflineQueueStore().clearMemory()` dans la séquence de démontage, avant l'appel réseau de révocation.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-4-notifier-l-utilisateur-du-resultat-de-synchronisation.md`
  summary: Le store `escrow` n'est jamais réinitialisé à la déconnexion : après un `logout()` suivi d'une connexion dans le même onglet, les transactions et le détail de l'utilisateur précédent restent en mémoire jusqu'à ce qu'un refetch aboutisse.
  evidence: Pré-existant et indépendant de la notice : `stores/auth.js#logout()` (`:74-78`) n'efface que `token` et `user`, et aucun `$reset()` n'existe dans le repo (vérifié par grep). `transactions`, `currentDetail` — et désormais leurs horodatages `transactionsFetchedAt`/`currentDetailFetchedAt` — survivent donc à la bascule de session : `DashboardView` affiche les transactions de l'utilisateur précédent jusqu'à la résolution de `loadTransactions()`. La Story 4.4 cloisonne les *entrées* gelées par `meta.userId`, mais l'état contre lequel elles résolvent leur badge n'est pas cloisonné. Portée limitée en pratique (un id de transaction identifie une seule transaction, dont les deux utilisateurs seraient alors parties), mais la fenêtre d'affichage inter-session est réelle et se corrige au niveau de l'authentification, pas d'une notice.
  RÉSOLU 2026-07-26 (Story 1.9) : `endSession()` appelle `$reset()` sur les stores `escrow` et `evidence` dans les deux modes (déconnexion et expiration), horodatages de cache compris.
  status: RÉSOLU 2026-07-26 (Story 1.9) — **vérifié contre le code au balayage 2026-07-27**, l'entrée était restée ouverte alors qu'une story ultérieure l'avait close : `stores/session.js` appelle `useEscrowStore().$reset()` et `useEvidenceStore().$reset()` (et non un blanchiment écrit à la main).

- source_spec: `_bmad-output/implementation-artifacts/spec-4-4-notifier-l-utilisateur-du-resultat-de-synchronisation.md`
  summary: `sendTransactionEvent` compare les ids de transaction avec `===` là où tous les chemins voisins normalisent en `String(...)`, si bien qu'un id venu de la route (chaîne) empêche la pose du marqueur `_queuedEvent`.
  evidence: Pré-existant. `stores/escrow.js:129` — `if (this.currentDetail?.transaction?.id === id)` — alors que `:137`, `:140`, `:188` et `:192` utilisent `String(...) === String(...)`, précisément parce qu'un id de route est une chaîne quand celui du serveur est un nombre. Conséquence : le marqueur optimiste n'est pas posé, et la garde anti-affichage-optimiste de `SyncFailureNotice` (qui s'appuie dessus) ne s'arme pas pour cette ligne. Non corrigé ici : la spec 4.4 interdit explicitement de toucher aux marqueurs `_queued*`, dont l'annulation est livrée et prouvée par la Story 4.3.
  status: OUVERT — dette de cohérence, sévérité faible.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-4-notifier-l-utilisateur-du-resultat-de-synchronisation.md`
  summary: La suite de tests front lit un fichier source du backend par chemin relatif, si bien qu'elle échoue en ENOENT dans tout checkout ou contexte de build qui ne contient pas `backend/`.
  evidence: Pré-existant (motif introduit par la Story 4.3, élargi par 4.4 qui porte le nombre de tests concernés de 1 à 2). `src/utils/__tests__/replayFailure.spec.js:93-96,113-116` fait `readFileSync(resolve(process.cwd(), '../backend/src/main/java/com/zlecaf/escrow/domain/ErrorCode.java'))`. Ces gardes anti-dérive ont une vraie valeur (elles sont le seul lien entre l'enum Java et ses miroirs front), mais elles couplent `npm run test` à un checkout complet : lancée depuis un contexte de build Docker `frontend/` seul, ou dans un job CI qui ne clone que le front, la suite ne signale pas « pas de backend ici » — elle rapporte un échec de dérive trompeur. Le correctif (skip conditionnel avec message explicite, ou déplacement de la garde dans un job dédié) touche la stratégie de test des deux modules, hors périmètre d'une story front.
  status: RÉSOLU 2026-07-28 (bundle QUALITÉ-CI) — les deux gardes anti-dérive sont conservées (elles restent le seul lien entre l'enum Java et son miroir front), mais leurs deux causes d'échec sont désormais distinguées : `../backend` absent → `it.skipIf` avec la raison dans le nom du test ; `../backend` présent mais `ErrorCode.java` introuvable → échec BRUYANT disant que l'autorité du contrat a bougé et que la garde ne protège plus rien. Le second cas n'est pas sautable par construction : un skip silencieux y serait exactement le faux vert que ce bundle combat. **Prouvé par mutation sur un checkout front seul reconstitué** : ancien code → 2 échecs ENOENT trompeurs ; nouveau code → 26 passés / 2 sautés ; autorité déplacée → 2 échecs portant le message explicite. Suite complète toujours à 241/241.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-4-notifier-l-utilisateur-du-resultat-de-synchronisation.md`
  summary: Le mock d'`@/api/client` recopie en dur la valeur de `TOKEN_STORAGE_KEY`, si bien qu'un changement de cette constante laisserait la suite verte pendant que la production lirait une autre clé de localStorage.
  evidence: Pré-existant. `src/stores/__tests__/escrow.offline.spec.js:8` — `vi.mock('@/api/client', () => ({ default: { request: vi.fn() }, TOKEN_STORAGE_KEY: 'escrow_token' }))` — duplique la constante que `src/api/client.js:3` possède. Le re-export est nécessaire (`stores/auth.js:3` l'importe depuis le module mocké), mais `vi.importActual` le fournirait sans figer la valeur. Tant que les deux coïncident rien ne casse ; le jour où la clé change, le test ne le voit pas. Non corrigé ici : la Story 4.4 ne touche pas à cette constante, et le motif se corrige d'un coup sur l'ensemble des mocks du repo.
  status: OUVERT — dette de test, sévérité faible.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-5-recuperer-ses-preuves-apres-un-rejet-definitif.md`
  summary: Le commentaire de `frontend/src/components/__tests__/SyncFailureNotice.spec.js:241-242` annonce que « Story 4.5's recovery screen is what brings it back » à propos des entrées gelées sans `meta.userId` — or la Story 4.5 a explicitement refusé de les ressusciter (leur inventer un propriétaire rouvrirait la fuite inter-utilisateurs).
  evidence: Commentaire vérifié en place ; la Story 4.5 documente le refus dans ses Boundaries (« Ne pas faire réapparaître les entrées sans meta.userId ») et ses Design Notes. Le fichier n'a pas pu être corrigé dans la 4.5 : une de ses AC exige que les 36 tests de ce fichier restent inchangés, cette immuabilité étant la preuve que l'extraction vers `utils/frozenEntry.js` ne change aucun comportement. Portée : commentaire trompeur pour le prochain lecteur, aucun impact d'exécution.
  status: OUVERT — commentaire de test faux, sévérité faible ; à corriger avec le bundle DOC.


- source_spec: `_bmad-output/planning-artifacts/epics-production.md` (Story 1.1 — revue de code 2026-07-18)
  summary: Les comptes ADMIN auto-attribués AVANT le correctif de la Story 1.1 ne sont ni révoqués ni audités — le correctif ferme la porte pour l'avenir mais ne remédie pas l'existant, et un ADMIN rogue préexistant bloque en plus le seed légitime (`existsByRole(ADMIN)` vrai).
  evidence: `AdminBootstrap` s'appuie sur `existsByRole(ADMIN)`. Sur une base ayant tourné avec l'ancien code self-service, des privilèges illégitimes persistent. Correctif = migration Flyway de rétrogradation/audit des ADMIN non provisionnés + décision de politique (quels ADMIN sont légitimes) — décision humaine, hors périmètre de la story (fermer la faille ≠ nettoyer les données historiques). Notre base POC a été reconstruite (aucun rogue). Sévérité moyenne ; à traiter avant la bascule d'une base de prod ayant tourné avec l'ancien code.
  status: **DÉCISION TRANCHÉE le 2026-07-28** — pas de migration de données : il n'y en a pas à faire. Vérifié : aucune production n'existe (README, « cloud deploy out of POC scope » ; la Story 11-3 est au backlog), donc aucun compte légataire réel n'est à révoquer et une migration Flyway de rétrogradation serait un no-op sur la base vide qu'ouvrira la production. La moitié « remédier l'existant » de cette entrée est donc SANS OBJET.
  Ce qui restait réel, c'est le second membre que cette entrée avait déjà correctement nommé : « un ADMIN rogue préexistant bloque en plus le seed légitime ». Promouvoir une base de développement en production aurait conservé l'ADMIN auto-attribué ET supprimé l'amorçage légitime — l'amorçage croyant son travail fait. Discipline de déploiement, pas migration. Deux mesures : (1) critère d'entrée de la Story 11-3 — la production démarre sur une base VIDE ; (2) **garde livrée ce jour** — en profil `prod`, `AdminBootstrap` refuse de démarrer si un ADMIN existe dont l'email n'est pas celui de la configuration (`existsByRoleAndEmailNot`). Bornée au profil `prod` pour ne pas gêner le développement, et conditionnée à la présence de la configuration pour qu'un exploitant retirant les identifiants après le premier démarrage ne casse pas les redémarrages suivants. 3 tests dont la contre-épreuve du redémarrage légitime ; **prouvée par mutation** — garde neutralisée, seul `refusesToStartInProductionWhenAForeignAdminAlreadyExists` rougit. DURÉE DE VIE : à revoir en Story 7-2, qui rendra un second ADMIN légitime.

- source_spec: `epics-production.md` (Story 1.1 — revue de code 2026-07-18)
  summary: L'amorçage ADMIN ne gère ni la rotation du mot de passe ni un signal fort quand l'email configuré entre en collision avec un compte existant (la plateforme démarre alors sans ADMIN, sur un simple warn).
  evidence: `AdminBootstrap` est idempotent par rôle : une fois un ADMIN présent, changer `escrow.bootstrap.admin.password` puis redémarrer est un no-op silencieux ; une collision d'email produit un warn+skip. Relève de l'outillage d'admin complet de l'Epic 7 (générer/révoquer/roter, gestion des membres). Sévérité faible-moyenne.
  status: OUVERT — Epic 7 (back-office / administration).

- source_spec: `AuthService.register` (préexistant, surfacé par la revue de la Story 1.1)
  summary: `existsByEmail` puis `save` n'est pas atomique : deux inscriptions simultanées du même email peuvent produire une `DataIntegrityViolationException` non mappée (500) au lieu d'un 409.
  evidence: Pré-existant, NON introduit par la Story 1.1 (la garde de rôle n'y touche pas). Correctif = catcher la violation d'unicité et la mapper en `ConflictException` (409). Sévérité faible.
  status: OUVERT — **concurrence à l'inscription** (`existsByEmail` puis `save` non atomique) ; à traiter DANS l'Epic 2, qui réécrit le parcours d'inscription (2-4). Contrainte unique en base + traduction du conflit.


## Deferred from: code review de 11-1-pipeline-ci-cd-gate-tests (2026-07-25)

- W1 — `apk upgrade` au build des images = non-reproductibilité (2 builds du même commit ≠ mêmes images ; SBOM d'image tracé au run CI, pas au commit) et dépendance réseau aux miroirs Alpine. Trade-off assumé tant que rien n'est déployé ; résolution cible en Story 11.3 : base images épinglées par digest + processus de refresh (Renovate ou équivalent).
  status: OUVERT — bundle STACK-PROD, Story 11-3 (images épinglées par digest + refresh).
- W2 — RÉSOLU 2026-07-25 : protection posée (repo public) + exports JSON commités (branch-protection-*.json). Ré-exporter à chaque modification de la politique.
  status: RÉSOLU 2026-07-25 — protection posée et exportée en JSON versionnés.

## Deferred from: code review de 1-3-anti-bruteforce-authentification (2026-07-25)

- DEF1 — Race check-then-act du rate-limiter : la vérification (avant la chaîne) et le comptage (après la réponse) ne sont pas atomiques ; une rafale concurrente place un burst > seuil avant que le verrou ne s'arme. Inhérent à un filtre servlet ; l'attaquant gagne un burst borné puis est bloqué. Durcissement (token bucket atomique, ou pré-incrément optimiste) déféré — non trivial, gain marginal au MVP.
  status: OUVERT — bundle STACK-PROD, Story 11-8 (mesurer avant de dimensionner un token bucket atomique).
- DEF2 — Stratégie trusted-proxy / X-Forwarded-For pour le rate-limiter : BLOQUANT une fois le reverse proxy TLS posé (Story 1.4). Sans proxy de confiance, XFF est spoofable ; avec proxy mais sans config, toutes les requêtes portent l'IP du proxy → une seule clé partagée → DoS global potentiel. À traiter DANS la Story 1.4 (config du proxy + lecture XFF derrière une allowlist d'IP de confiance). Contrainte de séquencement : 1.4 doit intégrer ce point.
  status: OUVERT — bundle STACK-PROD, Story 11-3. **Vérifié au balayage 2026-07-27** : `server.forward-headers-strategy: framework` est posé GLOBALEMENT dans `application.yml:158`, sans allowlist de proxy de confiance. Le report annonçait « BLOQUANT une fois le reverse proxy TLS posé (Story 1.4) » — la 1.4 est livrée, la condition de blocage est donc ATTEINTE et non plus future. Fusionne le doublon de la revue 1.4/1.5 ci-dessous.
- DEF3 — Rate-limiting distribué multi-instances : l'état est en mémoire process (correct en mono-instance MVP). Post-Story 11.3 (stack de production) : si plusieurs répliques backend, déplacer le compteur vers un store partagé (Redis/bucket4j-hazelcast) sinon le seuil effectif est multiplié par le nombre de répliques.
  status: OUVERT — bundle STACK-PROD, Story 11-3 (compteur partagé si plusieurs répliques).

## Deferred from: code review of stories 1.4/1.5 (2026-07-25)

- Montage partiel de certificat TLS (crt XOR key) : `40-generate-tls-cert.sh` régénère les deux fichiers dès qu'une moitié manque → écrase un vrai certificat monté, ou crash si `/etc/nginx/certs` est en lecture seule. Cas rare (personne ne monte une demi-paire). Correctif = détecter l'état XOR et échouer explicitement. Sévérité faible.
  status: OUVERT — bundle STACK-PROD, Story 11-3. Sévérité faible.
- `proxy_pass http://backend:8080` (nginx) résout `backend` au démarrage et met le DNS en cache : si le conteneur backend est recréé avec une nouvelle IP, nginx tape l'ancienne → 502 jusqu'à reload. Impact faible en compose mono-hôte, réel pour l'ingress de prod. À traiter avec la Story 11.3 (`resolver` + variable dans `proxy_pass`).
  status: OUVERT — bundle STACK-PROD, Story 11-3 (`resolver` + variable dans `proxy_pass`).
- CSP backend `default-src 'self'` casse Swagger UI quand il est servi en direct par le backend en dev sur hôte (`mvn spring-boot:run`) : le bootstrap inline de Swagger est bloqué. Hors topologie conteneurisée livrée (ingress 404 + backend non publié). Correctif = assouplir la CSP sur les chemins de doc quand `docsExposed`, si le Swagger dev-hôte devient nécessaire.
  status: SKIP — limite strictement locale (Swagger servi en direct par `mvn spring-boot:run`, hors topologie conteneurisée livrée). Aucun gain de production ; à rouvrir seulement si le Swagger dev-hôte devient un besoin réel.
- (1.4, décision de revue) Redirection HTTP→HTTPS qui perd le port non standard : `return 301 https://$host$request_uri` renvoie vers le port 443 implicite. En local (HTTPS sur 8443) la redirection aboutit à une connexion refusée. Accepté : limite purement locale, la prod utilise 443 (redirection correcte) ; l'ingress de production définitif appartient à la Story 11.3.
  status: ACCEPTÉ — limite purement locale (HTTPS sur 8443) ; la production utilise 443, où la redirection est correcte. L'ingress définitif appartient à la Story 11-3.
- (1.4, décision de revue) Confiance en `X-Forwarded-For` sans allowlist de proxy (`server.forward-headers-strategy: framework`, posé globalement) : sûr dans la topologie compose livrée car le backend n'est pas publié (`expose` seul, joignable uniquement via le proxy). Accepté : le durcissement trusted-proxy (gating profil / RemoteIpValve + plages internes) va de pair avec le choix d'ingress réel de la Story 11.3. Prolonge le report DEF2 de la Story 1.3.
  status: OUVERT — **doublon de DEF2 (Story 1.3)**, fusionné au balayage 2026-07-27 : même défaut, même correctif, même story cible (11-3). Ne pas traiter deux fois.

## Deferred from: code review de 1-6-politique-mots-de-passe-revocation-jwt (2026-07-26)

- **Jeton révoqué → 403 non traité par l'intercepteur axios** (`frontend/src/api/client.js:31`, ne traite que 401). Prolonge l'entrée existante de la ligne 168 : celle-ci ne concernait que les jetons **expirés**, cas rare. La Story 1.6 rend l'état atteignable en routine — tout `revokeSessions` (logout ou changement de mot de passe sur un autre appareil) laisse le second appareil dans une UI authentifiée zombie : jeton et utilisateur persistent en `localStorage`, aucune redirection, chaque appel échoue silencieusement en 403. Correctif = traiter le 403 dans l'intercepteur, mais il touche l'authentification globale de l'app. Va avec la Story 1.9 (hygiène de session). **RÉSOLU 2026-07-26 (Story 1.9)** : l'intercepteur émet `escrow:session-expired` sur tout 401/403 **nu** avec jeton présent (un 403 codé `NOT_A_PARTY` et un 401 codé `AUTH_FAILED` ne déconnectent jamais) ; `stores/session.js` purge la mémoire et redirige vers `auth?redirect=<cible>` en conservant IndexedDB et le cache de lecture.
  status: RÉSOLU 2026-07-26 (Story 1.9) — voir le corps de l'entrée.
- **Lookup DB par requête authentifiée dans `JwtAuthFilter`** (`JwtAuthFilter.java:58`). Prix de l'approche A : un `findById` sur le chemin le plus chaud du système. Acceptable en MVP mono-instance, mais couple aussi la disponibilité de l'authentification à celle de la base — un incident de pool fait passer les utilisateurs pour révoqués. Correctif = cache court invalidé sur révocation. → Story 11.8 (tests de charge des parcours chauds), qui doit mesurer avant d'optimiser.
  status: OUVERT — Story 11-8 (tests de charge), qui doit mesurer avant d'optimiser.
- **Politique de mot de passe purement combinatoire** (`PasswordPolicy.java`). `Password123!` satisfait les 12 caractères et les 4 catégories : les mots de passe les plus devinés de tous les corpus de fuite sont conformes à la composition. Ni dictionnaire de mots de passe courants, ni vérification de compromission (type HIBP k-anonymity), ni score entropique (type zxcvbn). Le spec de la 1.6 ne l'a jamais demandé — à arbitrer lors de la Story 2.6 (reset de mot de passe), qui touche la même surface.
  status: OUVERT — **DÉCISION PRODUIT** attendue d'Oscard, à trancher avec la Story 2-6 (reset de mot de passe, même surface) : dictionnaire de mots de passe courants, vérification de compromission (HIBP k-anonymity) ou score entropique (zxcvbn) — ou aucun des trois, assumé.
- **Exception de filtre servlet hors enveloppe d'erreur** (`GlobalExceptionHandler.java`, `JwtAuthFilter.java:58`). La revue 1.6 a ajouté un `@ExceptionHandler(Exception.class)` qui garantit le champ `code` pour tout le chemin MVC, mais une exception levée *dans un filtre* (typiquement une `DataAccessException` sur le lookup `token_version`) s'échappe avant `@ControllerAdvice` : le `/error` du conteneur répond alors sans `code`, et le client AD-10 n'a rien à classifier. Correctif = `DefaultErrorAttributes` personnalisé injectant `code: INTERNAL_ERROR`, ou `ErrorController` dédié. Faible fréquence (suppose déjà un incident d'infrastructure), mais c'est le seul trou restant de l'enveloppe. À traiter avec la Story 11.4 (observabilité), qui touche la même surface.
  status: OUVERT — bundle OBSERVABILITÉ, Story 11-4. **Vérifié au balayage 2026-07-27** : aucun `DefaultErrorAttributes` ni `ErrorController` n'existe dans `backend/src/main/java`. C'est le dernier trou de l'enveloppe d'erreur — la Story 1.10 a couvert les rejets natifs de Spring MVC, pas ce qui est levé AVANT le `@ControllerAdvice`.

## Deferred from: code review de 1-7-chiffrement-au-repos (2026-07-26)

- source_spec: `_bmad-output/implementation-artifacts/spec-1-7-chiffrement-au-repos.md`
  summary: Aucun contrôle d'intégrité au téléchargement d'une preuve : le SHA-256 calculé au dépôt n'est écrit que dans `audit_logs.payload` et n'est jamais revérifié, `evidence_files` ne portant aucune colonne d'empreinte.
  evidence: Préexistant (la story n'a rien retiré), mais le chiffrement au repos en change la portée : le repli « objet sans enveloppe = legacy, rendu tel quel » (`MinioEvidenceStorage.load`) signifie qu'un objet déposé directement dans le volume de stockage, sans enveloppe, est servi comme preuve authentique avec un simple WARN. Le tag GCM protège les objets chiffrés ; il ne couvre pas le dépouillement. Correctif = colonne d'empreinte sur `evidence_files` + vérification à la lecture. Va avec la Story 1.8 (ingestion) ou 11.5 (sauvegarde/restauration).
  status: OUVERT — Story 11-5 (sauvegarde/restauration). **Vérifié au balayage 2026-07-27** : aucune colonne d'empreinte dans les migrations `V1..V9`, donc rien à revérifier à la lecture.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-7-chiffrement-au-repos.md`
  summary: Le téléchargement de preuve n'est plus streamé (l'objet est matérialisé pour être déchiffré) sans plafond mémoire JVM ni limite de téléchargements concurrents.
  evidence: GCM n'authentifie qu'au tag final : rendre un flux au fil de l'eau livrerait du clair non authentifié, le tampon est donc structurel, pas un raccourci. Mais ~3 copies vivantes par requête à 10 Mo, aucun `-Xmx` dans `backend/Dockerfile` et aucun `mem_limit` dans `infra/docker-compose.yml` : quelques dizaines de téléchargements simultanés suffisent à faire tomber la JVM. Correctif = borne mémoire conteneur + plafond de concurrence, à dimensionner sur mesure. → Story 11.3 (stack durcie) et 11.8 (tests de charge).
  status: RÉSOLU — vérifié contre le code au balayage 2026-07-27 : la revue de SUIVI de la même story a posé le plafond, l'entrée décrivait donc un état déjà dépassé. `MinioEvidenceStorage.MAX_OBJECT_BYTES` (= `EvidenceService.MAX_FILE_SIZE` + 4 Ko) est contrôlé DEUX fois — sur le `contentLength` déclaré avant tout chargement (502), puis sur ce qui est réellement lu (`readNBytes(MAX + 1)`), ce qui couvre aussi le cas d'un `contentLength` menteur. Aucun chemin ne matérialise plus un objet non borné.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-7-chiffrement-au-repos.md`
  summary: L'AAD des colonnes chiffrées est une constante globale (`escrow:db-secret`), identique pour `partner_hmac_keys` et `webhook_subscriptions` : rien ne lie une enveloppe à sa table ni à sa ligne.
  evidence: Choix documenté de la spec, mais son raisonnement (« un attaquant capable d'écrire en base a déjà gagné ») ne couvre pas le cas réaliste de la restauration PARTIELLE de sauvegarde, qui réinstalle silencieusement un secret révoqué, ni la recopie d'un secret webhook dans une ligne de clé partenaire. L'en-tête d'enveloppe est désormais authentifié (correctif de revue), la transplantation inter-lignes ne l'est pas. Correctif = convertisseur dédié par colonne portant table+colonne dans l'AAD, plus une reprise des enveloppes existantes. À poser avec l'outillage d'admin des clés (Epic 7, Story 7.1).
  status: OUVERT — Story 7-1 (outillage d'administration des clés).

- source_spec: `_bmad-output/implementation-artifacts/spec-1-7-chiffrement-au-repos.md`
  summary: Retirer une clé du trousseau pendant que l'application tourne casse TOUTES les livraisons de webhooks d'un coup, pas seulement l'abonnement concerné.
  evidence: `WebhookService.dispatch` charge `findByActiveTrue()` avant sa boucle : le déchiffrement a lieu à l'hydratation, donc une seule ligne indéchiffrable fait échouer la requête entière et annule l'isolation par abonnement documentée en tête de classe (`WebhookController.list()` répond 500 pour la même raison). Le démarrage refuse déjà cet état (le balayage échoue en nommant la table), la fenêtre est donc le retrait de clé À CHAUD — erreur d'exploitation contre laquelle le runbook met explicitement en garde. Correctif = dégrader par abonnement plutôt que par requête, à traiter avec la Story 8.3 (webhooks sortants fiables), qui réécrit ce chemin.
  status: OUVERT — Story 8-3 (webhooks sortants fiables), qui réécrit ce chemin.

## Deferred from: revue de suivi de 1-7-chiffrement-au-repos (2026-07-26)

- source_spec: `_bmad-output/implementation-artifacts/spec-1-7-chiffrement-au-repos.md`
  summary: La mise en service initiale du chiffrement au repos n'est pas déployable en rolling update : une instance restée en version antérieure lit l'enveloppe comme si c'était le secret, et rien dans la plateforme n'empêche cette séquence.
  evidence: Dès que la première instance 1.7 a scellé les tables, une instance pré-1.7 lit `esc:1:v1:…` comme valeur du secret HMAC : elle rejette toute signature partenaire (401) et signe ses webhooks avec une valeur fausse. Il n'existe pas de version intermédiaire « sait lire, n'écrit pas encore » — la bascule doit donc être complète, pas tournante. Le runbook le documente désormais noir sur blanc (patch de cette passe) mais aucun mécanisme ne le contraint. Sans conséquence aujourd'hui (topologie mono-instance livrée) ; à traiter avec la Story 11.3 (stack de production multi-instances), qui doit soit poser une version de transition en lecture seule, soit inscrire la bascule complète dans la procédure de déploiement.
  status: OUVERT — bundle STACK-PROD, Story 11-3 (topologie multi-instances).

- source_spec: `_bmad-output/implementation-artifacts/spec-1-7-chiffrement-au-repos.md`
  summary: Le balayage de scellement charge chaque table entière en une seule transaction, sans pagination, alors que la taille de `webhook_subscriptions` est pilotée par les appelants.
  evidence: `SecretsEncryptionBootstrap.sweepTable` fait `SELECT id, secret_key FROM <table> ORDER BY id` sans `LIMIT` et traite le tout dans une transaction unique — choix délibéré, c'est ce qui donne l'atomicité désormais prouvée par test. Mais aucun quota ne borne la création d'abonnements webhook par un compte authentifié (manque préexistant, `WebhookController.create`) : à grande échelle, le démarrage matérialise toutes les lignes en mémoire et tient un verrou long sur la table. Inoffensif aux volumes actuels (quelques lignes). Correctif = pagination par lots avec transaction par lot (au prix de l'atomicité globale) plus un quota d'abonnements ; à arbitrer avec la Story 8.3 (webhooks sortants fiables) et la Story 11.3.
  status: OUVERT — bundle STACK-PROD, Stories 11-3 et 8-3.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-7-chiffrement-au-repos.md`
  summary: La détection d'enveloppe binaire ne lit que les 4 octets de magic `ESCX` ; sa sûreté repose entièrement sur la liste blanche de types de la Story 1.1, et tomberait si celle-ci était élargie.
  evidence: `SecretCipher.isEnvelope(byte[])` ne vérifie ni la version, ni la longueur d'identifiant, ni la cohérence de taille — contrairement à son homologue texte, qui refuse explicitement un simple `startsWith` pour cette raison. Aujourd'hui inatteignable : `EvidenceContentValidator` n'admet que JPEG, PNG et PDF, dont les magics (`FFD8FF`, `89504E47`, `%PDF-`) ne valent jamais `ESCX` — un objet legacy ne peut donc pas être pris pour une enveloppe. Mais la garantie est CONTINGENTE : tout élargissement de la liste blanche à un format à en-tête libre (archives, conteneurs propriétaires, futur canal d'ingestion de la Story 1.8) rend le faux positif atteignable, et sa conséquence est irréversible — la preuve concernée devient définitivement illisible (502), en violation de la promesse WORM d'AD-25. Correctif = valider l'en-tête complet avant de conclure « enveloppe », ou porter le drapeau de chiffrement en base plutôt que de le renifler. Contrainte à honorer par la story qui élargira les types acceptés.
  status: OUVERT — sévérité faible. **Vérifié au balayage 2026-07-27** : `SecretCipher.BINARY_MAGIC` = `{'E','S','C','X'}`, 4 octets, conforme à la description.

## Deferred from: 1-8-scan-anti-malware-ingestion (2026-07-26)

- source_spec: `_bmad-output/implementation-artifacts/spec-1-8-scan-anti-malware-ingestion.md`
  summary: Le chemin de dépôt EN LIGNE affiche le `message` de l'enveloppe d'erreur et ignore complètement le champ `code` : il ne peut distinguer un rejet définitif d'une indisponibilité transitoire.
  evidence: Écart AD-10 **préexistant**, consigné ici parce que la Story 1.8 y ajoute deux codes dont la distinction compte. `EvidenceDeposit.vue:61` (`serverError.value = err.response?.data?.message || 'Upload failed. Please try again.'`) et le catch d'`uploadEvidence` (`stores/evidence.js`, même motif) affichent le texte serveur brut ; `classifyReplayFailure`/`describeFailure` (`utils/replayFailure.js`) ne sont câblés que sur la file de rejeu hors-ligne. Conséquence : un `400 EVIDENCE_MALWARE_DETECTED` (retirer la pièce) et un `502 SCAN_UNAVAILABLE` (réessayer) sont rendus de la même façon, sans action de reprise différenciée — ce que l'epic 1 exige pourtant (« tout rejet affiche un motif ET une action de reprise »). L'AC « motif explicite » de cette story tient malgré tout, parce que le message serveur nomme le fichier refusé. Correctif = brancher le chemin en ligne sur `describeFailure`/`classifyReplayFailure` plutôt que sur le `message` nu ; il touche toute la surface d'affichage d'erreurs de l'app, pas seulement le dépôt. À traiter avec la Story 2.1 (i18n, qui réécrit ces mêmes libellés) ou la story de reprise d'erreurs de l'epic UX.
  status: OUVERT — **bundle EPIC-2** : à traiter DANS la Story 2-1 (i18n), qui réécrit exactement ces libellés. **Vérifié au balayage 2026-07-27** : `EvidenceDeposit.vue:61` lit toujours `err.response?.data?.message` et ignore `code`, donc un rejet définitif (retirer la pièce) et une indisponibilité transitoire (réessayer) s'affichent à l'identique — écart AD-10.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-8-scan-anti-malware-ingestion.md`
  summary: `ClamavMalwareScannerIntegrationTest` n'est pas exécutable sur un hôte arm64 sans pré-tirer l'image à la main : `clamav/clamav` ne publie que des manifestes `linux/amd64`.
  evidence: Constat d'environnement fait pendant l'implémentation (`docker pull clamav/clamav:1.4.3_base` -> « no matching manifest for linux/arm64/v8 »). Testcontainers 1.21 n'expose aucun moyen de demander une plateforme au moment du *pull* : le contournement est `docker pull --platform linux/amd64 clamav/clamav:1.4.3_base` une fois, après quoi l'image est dans le magasin local et Docker Desktop l'exécute en émulation (~60 s de démarrage au lieu de ~20 s). Documenté dans le Javadoc du test et dans le README. Sans effet sur une CI amd64. Correctif éventuel = image ClamAV multi-arch (aucune officielle aujourd'hui) ou construction locale ; à réexaminer avec la Story 11.1 (CI lite), qui fixera l'architecture des runners.
  status: RÉSOLU 2026-07-28 (bundle QUALITÉ-CI) — le résiduel routé ici était « une CI amd64 exécute le test pour de vrai ». C'est le cas et ce l'était déjà : la garde n'est PAS conditionnée à la présence de l'image mais à l'**architecture** (`ClamavMalwareScannerIntegrationTest:100-103` — `if (!arm) return;`), et les deux jobs de la CI tournent sur `ubuntu-latest`, donc amd64, où la garde ne s'applique jamais et Testcontainers tire l'image normalement. Vérifié en plus sur la machine de dev arm64 : la suite du 2026-07-28 rapporte `Skipped: 0` et le rapport surefire montre le test **exécuté** (2 tests, 18,90 s), l'image ayant été tirée une fois à la main comme la javadoc le prescrit. Aucune action d'outillage ne restait : rien à changer dans les runners.

## Deferred from: code review de 1-8-scan-anti-malware-ingestion (2026-07-26)

- **Inondation de la table d'audit par les détections (D2 de la revue 1.8).** Chaque détection écrit une ligne `EVIDENCE_REJECTED_MALWARE` sans borne, et sur la route partenaire le nonce n'est consommé qu'après un dépôt RÉUSSI (`PartnerEvidenceService`) : la même requête signée se rejoue donc indéfiniment. Un déposant authentifié peut grossir sans limite une table append-only à rétention ≥ 5 ans avec un seul PDF porteur d'EICAR. La story a appliqué l'argument d'inondation au chemin subi (pannes de scanner, non auditées) et l'a ignoré sur le chemin choisi par l'attaquant. Correctifs candidats : déduplication par sha256, extension du limiteur de la Story 1.3 aux routes de dépôt, consommation du nonce même au rejet. → ~~**Story 1-10**~~ **RE-ROUTÉ 2026-07-26 (par la Story 1.10)** : cet item n'est pas de l'énumération mais de l'**épuisement de ressource**. L'anti-énumération traite ce qu'une réponse *révèle* ; ici rien n'est révélé — un déposant légitimement autorisé écrit trop. Les deux problèmes partagent une surface, pas un correctif : aucune des mesures candidates (déduplication par sha256, extension du limiteur, consommation du nonce au rejet) ne change ce qu'une réponse apprend à l'appelant, et les prendre ici aurait élargi la story à une politique de quota sans mandat. → relève de l'extension du limiteur de la **Story 1.3** aux routes de dépôt, ou d'une story d'opérabilité.
  status: OUVERT — **DÉCISION** attendue d'Oscard (D2 rouverte) : déduplication par sha256, limiteur sur les routes de dépôt, ou consommation du nonce même en cas de rejet. Table append-only à rétention ≥ 5 ans, donc la dette grossit avec l'usage.
- **Aucune sonde de santé pour la dépendance antivirus (D4 de la revue 1.8).** `depends_on: service_healthy` ne gate que le DÉMARRAGE. Si clamd meurt ensuite — le mode de panne le plus probable selon le runbook — le backend reste `UP`, le healthcheck du conteneur (grep `UP`) reste vert, orchestration et tableaux de bord aussi, pendant que 100 % de l'ingestion échoue en 502. Correctif = `HealthIndicator` dédié exposé sur `/actuator/health`. → **Story 11-4** (observabilité).
  status: OUVERT — bundle OBSERVABILITÉ, Story 11-4. **Vérifié au balayage 2026-07-27** : aucun `HealthIndicator` dans le backend. Un clamd mort après démarrage laisse le backend `UP` pendant que 100 % de l'ingestion tombe en 502.
- **Aucune borne sur les scans concurrents.** Au-delà du `MaxThreads` de clamd (12 par défaut, non configuré dans le compose), les dépôts légitimes tombent en 502 pendant que le conteneur se déclare `healthy`. Correctif = sémaphore côté adaptateur et/ou `MaxThreads`/`MaxQueue` dimensionnés. → **Story 11-8** (tests de charge), qui doit mesurer avant de dimensionner.
  status: OUVERT — Story 11-8 (mesurer avant de dimensionner).
- **Scan synchrone dans le verrou pessimiste, sans budget de lot.** 20 fichiers × 30 s de timeout de lecture ≈ 10 min de verrou `SELECT … FOR UPDATE` sur la ligne escrow, bloquant toute transition concurrente ; nginx (60 s par défaut) rend d'ailleurs un 504 nu au lieu du 502 promis. Passer l'ingestion en asynchrone est une décision d'architecture, pas un correctif de revue. → à arbitrer avec le spine.
  status: OUVERT — **DÉCISION D'ARCHITECTURE** attendue : l'ingestion asynchrone n'est pas un correctif de revue. À porter au spine avant l'Epic 4.
- **`clamav/clamav:1.4.3_base` hors du garde-fou chaîne d'approvisionnement.** Ni entrée SBOM, ni gate Trivy (qui ne scanne que `escrow-backend:ci` et `escrow-frontend:ci`), épinglé par tag et non par digest — et l'image se déclare elle-même périmée (1.4.3 face à 1.4.5 recommandée). C'est un parseur C exposé à des entrées hostiles qui entre dans la frontière de confiance sans le contrôle prévu pour ça. → **Story 11-6** (résilience des dépendances).
  status: OUVERT — Story 11-6 (résilience des dépendances). **Vérifié au balayage 2026-07-27** : `infra/docker-compose.yml:110` épingle bien par tag `1.4.3_base` et non par digest.

## Deferred from: code review de 1-9-hygiene-session-appareil-partage (2026-07-26)

- source_spec: `_bmad-output/implementation-artifacts/spec-1-9-hygiene-session-appareil-partage.md`
  summary: La déconnexion n'est pas propagée aux autres onglets du même appareil : un second onglet reste dans une UI authentifiée zombie, sans redirection.
  evidence: Préexistant, non introduit par cette story mais non fermé par elle. Après la déconnexion dans l'onglet 1, l'onglet 2 conserve `auth.token` en mémoire (`isAuthenticated` vrai, la garde de routeur passe) alors que `localStorage` est vide. Son intercepteur de requête n'attache donc plus d'en-tête, le backend répond 403 nu — mais la garde « jeton présent » de `api/client.js:60` lit `localStorage`, n'y trouve rien, et **n'émet pas** `escrow:session-expired`. Aucun écouteur `storage` n'existe dans `frontend/src` (grep). Avant la story le résultat était identique (l'intercepteur ne traitait que 401), donc aucune régression — mais le passage de `window.location.href` à une navigation de routeur retire le rechargement qui masquait parfois le cas. Correctif = écouteur `storage` sur la clé de jeton propageant `endSession({reason:'expired'})` aux autres onglets ; décision de conception (quel onglet fait autorité, que faire d'un rejeu en vol) hors périmètre d'une story de session.
  status: OUVERT — bundle SESSION-PARTAGÉE. **Vérifié au balayage 2026-07-27, reconfirmé le 2026-07-28** : ni `BroadcastChannel` ni écouteur `storage` dans `frontend/src`. **Routage corrigé 2026-07-28** : commandée par la décision D4 (session non persistante par défaut + expiration d'inactivité) — implémentation de cette politique, pas correctif indépendant. Même story que D4.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-9-hygiene-session-appareil-partage.md`
  summary: `caches.delete('escrow-api-cache')` détruit les réponses mais laisse l'index d'expiration Workbox, qui conserve les URL `/api/` visitées par l'utilisateur partant.
  evidence: `vite.config.js:57` déclare `expiration: { maxEntries: 100, maxAgeSeconds: 86400 }` sur la règle `escrow-api-cache`, ce qui installe l'`ExpirationPlugin` de Workbox et sa propre base IndexedDB (store `cache-entries`, lignes clefées `escrow-api-cache|<url>`). `purgeReadCache()` (`stores/session.js`) supprime le bucket Cache Storage, jamais cette base : après une déconnexion « qui purge tout », les identifiants de transaction ouverts par le partant et leurs horodatages restent lisibles sur l'appareil jusqu'à l'éviction (24 h ou 100 entrées). Portée = métadonnées d'URL, pas de contenu — mais l'affirmation d'hygiène de la story est, elle, sans réserve. Correctif = ouvrir la base d'expiration par son nom et la supprimer, ou retirer l'option `expiration` de cette règle (au prix d'un cache non borné). Dépend d'un détail interne de Workbox : à traiter avec la Story 11.3 (stack de production) ou la revue PWA.
  status: OUVERT — bundle SESSION-PARTAGÉE. **Routage corrigé 2026-07-28** : service worker / Workbox, PAS la surface de l'Epic 2 — l'évidence de l'entrée le disait déjà (« à traiter avec la Story 11.3 »). Reste à la 11-3 / revue PWA.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-9-hygiene-session-appareil-partage.md`
  summary: Les deux seuls appelants de production des nouvelles primitives — le bouton de déconnexion et le câblage de `main.js` — n'ont aucun test.
  evidence: `endSession({reason:'logout'})` n'est atteint en production que depuis `views/DashboardView.vue:54`, et `installSessionExpiryListener` que depuis `main.js:18`. Aucun `DashboardView.spec.js` n'existe (11 fichiers de spec au total). Une régression de câblage — `router.replace` retiré, ou `installSessionExpiryListener` déplacé après `app.mount` — laisserait toute la fonctionnalité verte. Ironie relevée en revue : la story justifie le passage de `window.location.href` à `router.replace` par « une navigation de routeur rend le parcours testable », puis ne le teste pas. Correctif = un test de composant sur le bouton de déconnexion (le harnais `@vue/test-utils` existe depuis la Story 4.4).
  status: OUVERT — **autonome, faisable sans attendre D4** (routage corrigé 2026-07-28). Ne dépend d'aucune décision : le harnais `@vue/test-utils` est confirmé opérationnel (bundle QUALITÉ-CI, 2026-07-28) et le bouton de déconnexion existe déjà. Seule réserve : la story 2-3 (layout trois espaces) déplacera probablement ce bouton — écrire le test AVEC 2-3 évite de le réécrire aussitôt.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-9-hygiene-session-appareil-partage.md`
  summary: Le rejeu lancé par `adoptSession()` est un no-op si un `flush()` de la session précédente détient encore le verrou `flushing` ; les entrées du nouvel utilisateur attendent alors un événement `online` ou un rechargement.
  evidence: `flush()` sort immédiatement sur `if (this.flushing || ...)` (`stores/offlineQueue.js`). Depuis le correctif de revue, une bascule d'utilisateur en cours de boucle fait sortir l'ancien run par `break` — le verrou se libère donc rapidement — mais `adoptSession()` a déjà appelé `flush()` et n'y revient pas. Fenêtre étroite (elle suppose une déconnexion/reconnexion pendant un upload multipart) et conséquence bornée (rejeu différé, jamais perdu ni mal attribué). Correctif = mémoriser la promesse de rejeu en cours et la chaîner, ou re-déclencher `flush()` une fois le verrou relâché.
  status: OUVERT — bundle SESSION-PARTAGÉE. **Routage corrigé 2026-07-28** : file hors-ligne, surface de l'Epic 9. Fenêtre étroite (déconnexion/reconnexion pendant un envoi) et conséquence bornée (rejeu différé, jamais perdu) — ne bloque pas l'Epic 2.

## Deferred from: revue de suivi de 1-9-hygiene-session-appareil-partage (2026-07-26)

- source_spec: `_bmad-output/implementation-artifacts/spec-1-9-hygiene-session-appareil-partage.md`
  summary: Un jeton présent avec un `escrow_user` illisible est traité comme « personne n'est connecté » par la file offline : plus rien n'est hydraté ni rejoué, et les nouvelles entrées naissent orphelines, donc supprimées à la déconnexion suivante.
  evidence: `loadStoredUser()` (`stores/auth.js:17-24`) rend `null` sur tout échec de `JSON.parse` alors que `TOKEN_STORAGE_KEY` survit — divergence **préexistante**, que `utils/frozenEntry.js:43-59` documente explicitement comme un état que le code doit supporter. `isAuthenticated` valant `Boolean(token)`, la garde de routeur laisse passer et l'API fonctionne normalement ; mais `init()` et `hydrate()` lisent `user?.id == null` comme « pas de session », si bien que `RecoveryView` annonce « Nothing to recover » pendant que les preuves dorment en IndexedDB, et que chaque nouvelle mise en file (`stores/escrow.js:82,127,185`) estampille `userId: undefined`. Avant cette story ces entrées étaient encore rejouées (seul leur affichage était filtré) : la conséquence est donc nouvelle, la cause ne l'est pas. Correctif = distinguer les trois états plutôt que deux — soit `auth.js` efface le jeton quand le profil est illisible (la session devient inutilisable pour le scoping), soit la file signale explicitement « session incohérente » au lieu de se taire. Décision de sémantique d'authentification, pas correctif de revue : à trancher avec la story qui reprendra le cycle de vie du jeton (rafraîchissement / expiration côté client).
  status: OUVERT — bundle SESSION-PARTAGÉE. **Routage corrigé 2026-07-28** : commandée par la décision D4 — la sémantique à trois états du jeton se décide avec la politique de persistance, pas avant. Même story que D4.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-9-hygiene-session-appareil-partage.md`
  summary: Le mode de défaillance dominant de l'appareil partagé reste ouvert : fermer l'onglet sans se déconnecter laisse la session utilisable jusqu'à 24 h pour la personne suivante.
  evidence: `backend/src/main/resources/application.yml:44` fixe `ttl-seconds: 86400`, le JWT vit dans `localStorage` (il survit à la fermeture de l'onglet comme au redémarrage du navigateur), et il n'existe ni expiration d'inactivité, ni contrôle de durée de vie au démarrage, ni déconnexion sur `unload`. A se connecte à une borne partagée, termine, ferme l'onglet et s'en va ; B rouvre l'application quelques minutes plus tard et est **silencieusement authentifié en tant que A** — transactions, preuves et cache de lecture compris. `beginSession` ne tourne pas dans ce scénario, donc ni le marqueur `escrow_last_user` ni la purge de cache ne s'exécutent. L'AC de la story ne couvre que « A quitte la session — déconnexion explicite ou expiration subie », elle est donc formellement tenue ; mais la NFR-P8 « hygiène de session sur appareil partagé » ne l'est pas pour ce chemin, et c'est le plus probable des trois. Correctif = expiration d'inactivité côté client (minuterie + contrôle au démarrage) et/ou réduction du TTL avec rafraîchissement, plus une décision produit sur `sessionStorage` vs `localStorage` pour les postes partagés. Arbitrage produit + backend : à porter par une story dédiée de l'epic 1 ou par la revue NFR.
  status: OUVERT — **DÉCISION PRODUIT TRANCHÉE le 2026-07-28 par Oscard : les deux mesures, pas l'une ou l'autre.** (1) Jeton en `sessionStorage` par DÉFAUT — il meurt avec l'onglet, ce qui ferme le chemin dominant à la racine — avec une option explicite « rester connecté » qui bascule en `localStorage` pour les appareils personnels ; (2) expiration d'inactivité côté client (minuterie + contrôle au démarrage), qui couvre l'utilisateur resté connecté sur son propre poste et qui s'éloigne. La première seule laissait ce second chemin ouvert ; la seconde seule laissait un onglet rouvert DANS la fenêtre d'inactivité authentifier silencieusement la personne suivante.
  **Faits revérifiés contre le code le 2026-07-28** (l'entrée n'est pas périmée) : `application.yml:44` porte toujours `ttl-seconds: 86400`, `stores/auth.js:41` écrit le jeton en `localStorage`, et aucune expiration d'inactivité n'existe dans `frontend/src`.
  **Cette décision commande quatre autres entrées de ce même bundle** — propagation inter-onglets, sémantique du profil illisible, timeout de `logoutUser`, époque de session pour les courses de lecture. Elles ne sont pas des correctifs indépendants mais quatre implémentations de la politique ci-dessus : à traiter dans la MÊME story, pas dispersées. Reste à décider où elle atterrit (story dédiée vs rattachement à 2-6, dont les AC ne couvrent aujourd'hui que la révocation au changement de mot de passe).

## Deferred from: 2e revue de suivi de 1-9-hygiene-session-appareil-partage (2026-07-26)

- source_spec: `_bmad-output/implementation-artifacts/spec-1-9-hygiene-session-appareil-partage.md`
  summary: `logoutUser` est un `fetch` sans timeout ni `AbortController`, et `DashboardView` attend `endSession` avant de naviguer : sur portail captif, l'utilisateur reste indéfiniment sur un tableau de bord vidé, sans issue.
  evidence: `api/auth.js:36-43` émet un `fetch` nu (`keepalive: true`, aucun `signal`). `stores/session.js` l'attend en dernier — l'ordre est correct et prouvé par test, tout le local est purgé avant — puis `views/DashboardView.vue:54` attend `endSession` avant `router.replace`. Un réseau qui ne répond jamais (portail captif, DNS suspendu) suspend donc la navigation aussi longtemps qu'il suspend la requête. L'hygiène de l'appareil est intacte (rien de lisible ne subsiste à l'écran ni sur disque), la conséquence est purement d'usage : écran mort, aucun retour, aucun bouton désactivé. **Préexistant** — avant la story, `await auth.logout()` attendait déjà la même requête. Correctif = borner l'attente (`Promise.race` avec un délai court : `keepalive` garantit que la requête aboutit même après la navigation) ou lancer la révocation sans l'attendre. Touche une décision explicite de la revue 1.6 et une contrainte « Never » de la spec 1.9 (« ne pas remplacer `logoutUser` par un appel axios ») : à rouvrir avec la story qui reprendra le cycle de vie du jeton.
  status: OUVERT — bundle SESSION-PARTAGÉE. **Vérifié au balayage 2026-07-27, reconfirmé le 2026-07-28** : `api/auth.js` ne porte que `keepalive: true`, aucun `signal` ni timeout. **Routage corrigé 2026-07-28** : commandée par la décision D4 (session non persistante par défaut + expiration d'inactivité) — implémentation de cette politique, pas correctif indépendant. Même story que D4.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-9-hygiene-session-appareil-partage.md`
  summary: Une lecture émise par A peut atterrir dans les stores après la connexion de B : `escrow.loadTransactions` écrit sans garde de session, et le `$reset()` d'`evidence` rembobine son compteur anti-course à 0.
  evidence: `stores/escrow.js` écrit `this.transactions = await fetchTransactions()` sans comparer la session au retour ; `stores/evidence.js` se protège des courses par un `loadSeq` monotone, que `$reset()` ramène à 0 — si bien qu'une lecture de A partie avec `seq=1` redevient égale au `loadSeq=1` de la première lecture de B et est acceptée. La story pose deux `$reset()` (à la fin de session **et** au début, ce dernier ajouté en revue) qui réduisent fortement la fenêtre, mais aucun des deux ne peut annuler une réponse déjà en vol : entre le `$reset()` de `beginSession` et la première lecture de B, une réponse de A qui arrive est écrite et rendue. L'AC 1 revendique « à aucun instant du parcours », ce qui n'est donc pas tenu au sens strict. Écriture non gardée **préexistante** (stores des epics 2/4), conséquence sur appareil partagé nouvelle. Correctif = une époque de session (compteur monotone porté par `stores/session.js`, capturé à l'émission et comparé à la résolution dans chaque action de lecture), ce qui touche des stores que la spec 1.9 s'interdit de modifier. À porter par une story dédiée ou par la revue NFR-P8.
  status: OUVERT — bundle SESSION-PARTAGÉE. **Routage corrigé 2026-07-28** : commandée par la décision D4 — l'époque de session est la primitive que cette politique doit fournir. Même story que D4.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-9-hygiene-session-appareil-partage.md`
  summary: Une réponse `/api/` déjà en vol au moment de `purgeReadCache()` recrée `escrow-api-cache` derrière la purge, avec la réponse de l'utilisateur qui part.
  evidence: La règle Workbox de `vite.config.js:50-58` est un `NetworkFirst` : le service worker écrit la réponse en cache **à sa réception**, dans son propre contexte d'exécution. `caches.delete(READ_CACHE_NAME)` supprime le bucket à l'instant T ; une requête `/api/` partie avant T et résolue après recrée le bucket et y dépose sa réponse, qui appartient à la session qui vient de se terminer. La déconnexion navigue immédiatement vers `/auth`, ce qui rend la fenêtre courte mais non nulle (une liste de transactions lente suffit). Le marqueur `escrow_last_user` limite le dommage — un utilisateur différent purge à nouveau à sa connexion — mais pas pour une reconnexion du même utilisateur après une déconnexion explicite, où le cache est censé avoir disparu. Correctif = faire porter la purge par le service worker lui-même (message `postMessage` traité dans `sw.js`), ce que la spec exclut explicitement (« Block If » : passer en `injectManifest` changerait la stratégie PWA livrée). À traiter avec la Story 11.3 / la revue PWA.
  status: OUVERT — bundle SESSION-PARTAGÉE. **Routage corrigé 2026-07-28** : service worker, PAS la surface de l'Epic 2 — comme l'autre entrée Workbox, et pour la même raison que son évidence donnait déjà (« à traiter avec la Story 11.3 / la revue PWA »). La purge doit être portée par le service worker lui-même, ce qu'aucune story de l'Epic 2 ne fait. Reste à la 11-3.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-9-hygiene-session-appareil-partage.md`
  summary: Aucune configuration ESLint n'existe dans `frontend/`, alors que le code porte six directives `// eslint-disable-next-line` : ces commentaires décrivent une barrière qui n'existe pas.
  evidence: Ni `eslint.config.*`, ni `.eslintrc.*`, ni script `lint` dans `package.json` (`npx eslint` échoue). Les directives présentes (`no-control-regex` dans `views/AuthView.vue`, cinq `no-await-in-loop` dans `stores/offlineQueue.js`) donnent au lecteur l'impression qu'un analyseur valide ces choix, alors que rien ne tourne. Conséquence concrète mesurée à cette passe : c'est exactement le genre de défaut qu'un `lint` aurait attrapé — la classe de caractères de contrôle écrite en octets bruts avait transformé `AuthView.vue` en fichier binaire (invisible à `grep`, indiffable par `git`) et la suite restait verte. **Préexistant** et hors périmètre d'une story de session. Correctif = installer `eslint` + `eslint-plugin-vue`, un script `lint` et son gate CI. À porter par la Story 11-1 (CI lite) ou une story d'outillage frontend.
  status: RÉSOLU 2026-07-28 (bundle QUALITÉ-CI) — `frontend/eslint.config.js` (config plate), script `npm run lint` = `eslint . --max-warnings 0`, gate CI posé **avant** les tests dans le job frontend. Jeu de règles `js/recommended` + `vue/flat/essential` + `no-await-in-loop` activée délibérément (c'est la règle que visent cinq des six directives), et `reportUnusedDisableDirectives: 'error'` pour que le décalage constaté ici ne puisse pas se reformer. **Prouvé par mutation, dans les deux sens** : (a) règle `no-await-in-loop` retirée → les 6 directives rougissent en « Unused eslint-disable directive » ; (b) directive retirée d'`AuthView.vue:43` → `no-control-regex` mord (« Unexpected control character(s) : \x00, \x1f »). Les six directives décrivent donc désormais une barrière qui existe. Deux défauts réels trouvés et corrigés au passage : `props` mort dans `NewTransactionModal.vue`, `route`/`useRoute` morts dans `TransactionDetailView.vue`. Arbitrage assumé : `flat/essential` et non `flat/recommended`, dont les 134 avertissements de formatage auraient réécrit la mise en page de 15 composants la veille du jour où l'Epic 2 les remplace.

## Deferred from: 1-10-anti-enumeration-ressources (2026-07-26)

- source_spec: `_bmad-output/implementation-artifacts/spec-1-10-anti-enumeration-ressources.md`
  summary: **Preuve wallet due.** L'AC d'epic exige l'indistinguabilité « sur transactions, preuves **et wallets** » ; la partie wallet n'est pas livrée et ne pouvait pas l'être.
  evidence: Aucune entité, table, endpoint ni garde wallet n'existe dans le dépôt — `WalletService.getOrCreate` relève d'AD-13 et de l'Epic 4. Fabriquer ici une ressource wallet pour pouvoir la tester aurait préempté une décision d'architecture ouverte (cf. entrée « trou normatif » ci-dessous) et débordé d'un epic entier. Ce qui était livrable l'a été intégralement : la fabrique réutilisable (`ApiExceptions`), la convention écrite là où les agents la lisent (`project-context.md`) et la propriété prouvée sur les deux familles de ressources qui existent. **À la charge de la story wallet d'Epic 4** : sa garde d'appartenance doit jeter `ApiExceptions.walletNotFound()` (nouvelle fabrique, même patron) et un test doit comparer les deux réponses en entier, comme `AntiEnumerationIntegrationTest` le fait pour les transactions.
  status: OUVERT — dette de preuve nommément portée par la story wallet d'Epic 4.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-10-anti-enumeration-ressources.md`
  summary: **Canal temporel non couvert.** Les deux branches désormais indistinguables en contenu ne le sont pas en *durée* : elles ne font pas le même nombre de requêtes SQL.
  evidence: Sur le canal partenaire, le chemin « société non partie » exécute jusqu'à deux `users.findById` de plus (`TransactionAccess.belongsToCompany` sur l'acheteur puis sur le vendeur) que le chemin « transaction inexistante », qui s'arrête au `findByIdForUpdate` vide. Côté JWT l'écart est plus faible mais non nul (le `resolveRole` n'est atteint que si la transaction existe). Un attaquant patient qui mesure les temps de réponse retrouve donc partiellement l'oracle que le contenu ne donne plus. Explicitement hors périmètre de la story (« ne pas prétendre fermer le canal temporel »). Correctifs candidats : égaliser le travail des deux branches, ou introduire une latence plancher — tous deux coûteux et à décider au niveau plateforme, pas dans une garde. Sévérité faible (exige beaucoup d'échantillons et une session valide), mais réelle et non mesurée à ce jour. — **Complété en revue (2026-07-26) : la variante « contention de verrou », distincte et plus facile à exploiter que la latence brute.** `EscrowTransactionRepository.findByIdForUpdate` porte `@Lock(LockModeType.PESSIMISTIC_WRITE)` et est le chargement utilisé par cinq chemins d'écriture (`EscrowService:109,164` ; `EvidenceService:127,159,519`). Deux sondes **concurrentes** sur un identifiant **réel** se sérialisent donc sur un `SELECT ... FOR UPDATE`, alors que deux sondes sur un identifiant fictif ne verrouillent rien et restent parallèles. L'écart n'est plus une différence de coût de quelques requêtes noyée dans le bruit réseau : c'est un blocage, provoqué **à la demande** par l'attaquant (il choisit d'émettre N sondes simultanées) au lieu d'être mesuré sur un grand échantillon, et amplifié à volonté en allongeant la transaction qui détient le verrou. Sévérité supérieure à celle du surcoût de requêtes ci-dessus, et non couverte par une latence plancher — laquelle masque une durée moyenne, pas une sérialisation. Correctif candidat distinct : ne prendre le verrou pessimiste qu'**après** la garde d'appartenance (charger d'abord sans verrou pour arbitrer l'accès), ce qui déplace le coût sans le supprimer et demande de revérifier les invariants anti-double-dépense d'AD-25.
  status: OUVERT — durcissement, à trancher au niveau plateforme. Deux variantes distinctes : surcoût de requêtes (mesuré) et contention de verrou (provoquée).

- source_spec: `_bmad-output/implementation-artifacts/spec-1-10-anti-enumeration-ressources.md`
  summary: **Oracle d'e-mail sur `POST /api/v1/escrow`.** La création d'une transaction distingue « aucun vendeur inscrit avec cet e-mail » (400) d'une création réussie — soit une énumération du fichier des utilisateurs.
  evidence: `EscrowService.create` répond `BadRequestException("No seller registered with that email")` quand `users.findByEmail` est vide. C'est une énumération d'utilisateurs, pas de ressources, et surtout c'est une **décision produit** avant d'être technique : la masquer signifie créer une transaction vers un destinataire non inscrit (invitation en attente) ou accepter un message volontairement vague qui dégrade un parcours de création légitime. Hors périmètre d'une story dont l'objet est l'anti-IDOR sur ressources existantes. → porté par les **stories 2.4 / 2.5 / 2.6** (onboarding et invitation), qui doivent en faire un arbitrage explicite plutôt qu'un effet de bord.
  status: **DÉCISION TRANCHÉE le 2026-07-28** — routage CORRIGÉ : ni 2.4, ni 2.5, ni 2.6, mais la **Story 5-1**. Cette entrée avait vu juste sur le fond (« la masquer signifie créer une transaction ») sans voir que c'est exactement ce que 5-1 fait déjà : son AC dit qu'un email ne correspondant à aucun compte déclenche une INVITATION et laisse la transaction « en attente d'acceptation ». Les deux branches convergent donc, et l'oracle disparaît par construction — aucune story de l'Epic 2 n'y touche. Décision : NE PAS corriger sur place. Patcher l'endpoint POC aujourd'hui reviendrait à inventer une réponse indistinguable, plus son test jumeau, pour un flux réécrit deux epics plus loin. Risque borné : aucune production n'existe. AC d'indistinguabilité (deux réponses entières identiques, test jumeau sur le gabarit `AntiEnumerationIntegrationTest`) AJOUTÉ aux AC de 5-1 dans `epics.md`, sous un encadré CONTRAINTE DURE : **si le chemin invitation était reporté ou découpé, l'oracle devrait être fermé séparément avant toute mise en production.**

- source_spec: `_bmad-output/implementation-artifacts/spec-1-10-anti-enumeration-ressources.md`
  summary: **Trou normatif du spine : aucun AD ne dit quelle garde protège un wallet.**
  evidence: Remonté au titre de la règle « toute contradiction entre une story et un AD est un conflit à remonter, pas à résoudre localement ». AD-3 énonce la garde d'appartenance en termes de « partie à une transaction » ; AD-13 scope le wallet à (company, currency) — une dimension où « partie à une transaction » n'a pas de sens. Rien ne tranche entre une garde de company (proche d'AD-20) et une garde de rôle interne (AD-30), ni ce que doit voir un MEMBER par rapport à un MANAGER. Tant que ce point est ouvert, la story wallet d'Epic 4 devra le trancher elle-même — c'est-à-dire prendre une décision d'architecture dans une story de fonctionnalité, exactement ce que le spine existe pour éviter. Correctif = amender le spine (nouvel AD ou extension d'AD-3) avant que la story wallet ne démarre.
  status: OUVERT — conflit de niveau architecture, à porter au spine.

## Deferred from: code review de 1-10-anti-enumeration-ressources (2026-07-26)

- source_spec: `_bmad-output/implementation-artifacts/spec-1-10-anti-enumeration-ressources.md`
  summary: **`GET /api/v1/webhooks/subscriptions` liste les abonnements de TOUTES les sociétés à tout utilisateur authentifié** — énumération inter-tenant des sociétés et de leurs points de terminaison.
  evidence: `WebhookController.list()` (`backend/.../web/WebhookController.java`) fait `subscriptions.findAll().stream().map(SubscriptionDto::from).toList()` : aucun contrôle d'appartenance, aucune garde, aucun filtre sur l'appelant. Le `SubscriptionDto` expose `companyId` et `targetUrl`. Aucun secret ne fuit (`secretKey` n'est pas dans le DTO), mais la réponse cartographie les sociétés partenaires de la plateforme et l'URL de rappel de chacune — exactement la classe de défaut que la Story 1.10 vient de fermer sur les transactions et les preuves, laissée ouverte sur une autre ressource. **Préexistant** et hors périmètre : la story bornait explicitement son objet aux transactions et aux preuves. Sévérité **haute** (lecture inter-tenant sans aucune authentification supplémentaire, sur une surface qui décrit l'infrastructure des partenaires). Correctif = scoper la liste sur la société de l'appelant via une garde centralisée (le patron d'AD-3), et servir le refus par une fabrique du même patron que `ApiExceptions.transactionNotFound()` — un abonnement d'une autre société doit être indistinguable d'un abonnement inexistant, pas « refusé ».
  status: RÉSOLU 2026-07-27 (rétrospective Epic 1, décision Oscard « corriger maintenant, hors story ») — `list()` est borné par `findByCompanyId(...)`, requête dérivée que l'appelant ne peut pas élargir. **Un second défaut, plus grave, a été trouvé sur la même classe et corrigé du même geste** : `create()` prenait le `companyId` dans le CORPS de la requête, si bien que tout compte authentifié pouvait s'abonner aux événements d'une société tierce vers sa propre URL de rappel — canal d'exfiltration, et non simple fuite de configuration. Le champ a été RETIRÉ de `SubscriptionRequest` plutôt que gardé (la société est dérivée du JWT) : un champ qu'on valide reste un champ qu'on peut oublier de valider au prochain endpoint. Appelant sans société : lecture vide, écriture refusée par un message constant portant sur sa propre identité — refus honnête au sens de l'exception documentée de la 1.10. 4 tests posés sur un endpoint qui n'en avait AUCUN, dont `verify(subscriptions, never()).findAll()` (ce qui est interdit est le chemin non borné, pas seulement le résultat filtré) ; vérifiés par mutation : gardes retirées → 2 rouges sur 4, restaurées → 481 tests verts. **Reste ouvert** : ce contrôleur parle encore au repository sans service — c'est le court-circuit qui a laissé passer le défaut, les gardes du projet vivant côté service. `WebhookSubscriptionService` porté en action item de rétro (Epic 7).

- source_spec: `_bmad-output/implementation-artifacts/spec-1-10-anti-enumeration-ressources.md`
  summary: **Un balayage d'identifiants ne laisse aucune trace** : l'oracle de contenu est fermé, la détection ne l'est pas.
  evidence: Le refus uniforme n'écrit ni ligne d'audit ni ligne de journal — et `AntiEnumerationIntegrationTest.probingWritesNothing` asservit désormais cette absence, **à raison** : une trace écrite pour le cas « transaction d'un tiers » et pas pour le cas « id inexistant » serait le même oracle décalé d'un canal (observable par tout porteur d'un accès aux journaux ou à la table d'audit), et un vecteur d'inondation d'une table append-only (AD-25). Par ailleurs `AuthRateLimitFilter` ne couvre que `/api/v1/auth/login`, `/register` et `/change-password` : **rien ne borne le débit sur la surface escrow**, donc un balayage peut être aussi rapide que le réseau le permet. Conséquence nette : un attaquant n'apprend plus rien d'une sonde isolée, mais peut en émettre autant qu'il veut sans jamais être vu. **Contrainte impérative pour tout correctif futur** : une journalisation ou un audit des refus doit être **symétrique** — les deux branches (inexistant / non-partie) doivent écrire la **même** ligne, avec le **même** contenu, sinon le canal des journaux rouvre l'oracle que la réponse HTTP vient de fermer. La piste sûre est un compteur par principal/IP sur les 404 de cette surface (qui ne distingue pas les deux causes) plutôt qu'une ligne par refus.
  status: OUVERT — détection et limitation de débit sur la surface escrow ; contrainte de symétrie à respecter par le correctif.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-10-anti-enumeration-ressources.md`
  summary: **Espace d'identifiants dense et monotone** : masquer l'existence dans la réponse laisse lire le compteur global de la plateforme et laisse l'espace de sondage balayable.
  evidence: `EscrowTransaction` (`domain/EscrowTransaction.java:15`) et `EvidenceFile` (`domain/EvidenceFile.java:17`) utilisent `@GeneratedValue(strategy = GenerationType.IDENTITY)`, donc des entiers séquentiels denses. Deux conséquences que l'anti-énumération de contenu ne touche pas : (a) toute partie légitime lit le **volume d'activité de la plateforme** sur son propre identifiant, et son évolution entre deux transactions donne un taux de croissance — une fuite d'information commerciale, pas technique ; (b) l'espace à sonder n'est pas `2^64` mais `[1, max]`, soit quelques milliers de requêtes pour le couvrir entièrement, ce qui rend le balayage praticable pour qui accepte le coût. Résiduel **connu et assumé** : la réponse ne confirme plus rien, mais l'effort de sondage reste faible. À peser conjointement avec l'entrée « aucune trace » ci-dessus — identifiants opaques (UUID/ULID publics) et détection sont deux réponses au même problème, et la première est une migration de schéma qui touche URL, DTO, file offline et liens persistés.
  status: OUVERT — résiduel connu ; arbitrage identifiants opaques vs détection, à porter au niveau plateforme.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-10-anti-enumeration-ressources.md`
  summary: **Mentions périmées de `NOT_A_PARTY`** : quatre fichiers de production frontend et six specs décrivent le rejeu inter-utilisateur en nommant un code que le backend n'émet plus.
  evidence: `frontend/src/stores/offlineQueue.js:154,289`, `frontend/src/stores/offlineQueue.idb.js:80` et `frontend/src/api/client.js:76` expliquent le scoping par utilisateur en termes de « le serveur répond NOT_A_PARTY » ; idem dans `stores/__tests__/session.spec.js:423`, `stores/__tests__/offlineQueue.spec.js:717,852` et `api/__tests__/client.spec.js:101,177`. Le **raisonnement reste exact** (le nouveau code `TRANSACTION_NOT_FOUND` est lui aussi PERMANENT, donc l'entrée gèle de la même façon) : ce sont des commentaires justes qui nomment un code mort, pas des défauts fonctionnels. Côté backend, `EscrowControllerDisputeTest:118` et `PartnerEvidenceControllerTest:109` construisent encore à la main `new NotFoundException("Transaction 42 not found")` pour armer leur mock — un littéral que la production n'émet plus, donc un mock qui ne ressemble plus à ce qu'il simule (leurs assertions, elles, portent bien sur `"Transaction not found"`). Hors périmètre : la spec bornait explicitement le frontend à `replayFailure.js`. Correctif = passe de rafraîchissement des commentaires, et remplacement des deux littéraux de test par `ApiExceptions.transactionNotFound()`.
  status: OUVERT — dette de documentation et de fidélité des mocks, sans conséquence fonctionnelle.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-10-anti-enumeration-ressources.md`
  summary: **Dérive du texte d'AD-10 dans les artefacts de planification** : les énumérations d'AD-10 ne décrivent plus l'enum réelle, et le test de contrat censé détecter la dérive décrivait donc un document périmé.
  evidence: Les énumérations d'AD-10 (`_bmad-output/planning-artifacts/epics-poc-evidence-upload.md` et les `epic-*-context.md`) listent encore `NOT_A_PARTY`, retiré par cette story, et omettent `WEAK_PASSWORD` (Story 1.6) et `EVIDENCE_MALWARE_DETECTED` (Story 1.8), tous deux ajoutés à l'ensemble permanent depuis. `ErrorCodeContractTest.AD10_PERMANENT` prétendait « les dix codes permanents que nomme AD-10 » : trois écarts sur dix, dans les deux sens. Traité côté code à cette passe (la javadoc décrit maintenant ce que l'ensemble **est** — la partition permanente que le client miroite — sans prétendre citer un document), mais **la dérive de l'artefact reste** : à corriger côté spine/planification, pas dans le code. Aucun artefact de planification n'a été modifié par cette revue.
  status: OUVERT — correction d'artefact de planification, à porter au spine.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-10-anti-enumeration-ressources.md`
  summary: **Aucune classe de base Testcontainers** : chaque suite d'intégration démarre son propre contexte Spring et son propre Postgres, avec un amorçage dupliqué au caractère près.
  evidence: `AntiEnumerationIntegrationTest` ajoute un contexte Spring et un conteneur Postgres de plus (le dépôt en compte une vingtaine portant `@Container`), et son bloc `@Container` + `@DynamicPropertySource` est identique à celui de `PasswordAndRevocationIntegrationTest:33-49` — même image `postgres:16-alpine`, mêmes trois `registry.add`. Ni classe de base commune, ni `withReuse(true)`, ni conteneur partagé au niveau JVM. Coût direct sur la durée de CI (le démarrage de conteneur domine le temps de ces suites) et coût de maintenance à chaque changement d'image ou de propriété. Relevé maintenant parce que la CI lite (**Story 11-1**) est la prochaine priorité déclarée et que ce coût s'y paiera à chaque exécution. Correctif = une classe de base abstraite portant le conteneur statique et le `@DynamicPropertySource`, dont héritent toutes les suites `@SpringBootTest` (un seul conteneur pour toute la JVM, réutilisé grâce au cache de contexte Spring).
  status: RÉSOLU 2026-07-28 (bundle QUALITÉ-CI) — `PostgresTestSupport` porte un conteneur Postgres UNIQUE pour toute la JVM ; les 18 suites appellent `registerDatabase(registry, MaSuite.class)` et ne déclarent plus ni `@Container`, ni `@Testcontainers`, ni le nom d'image (−188 lignes, +39). **Écart assumé avec le correctif que cette entrée prescrivait** : elle proposait « une classe de base abstraite dont héritent les suites », avec « un seul conteneur réutilisé grâce au cache de contexte Spring ». Le conteneur est bien mutualisé, mais PAS la base de données — chaque classe reçoit la sienne. Partager une base unique aurait rendu les suites `@SpringBootTest` (qui committent pour de vrai, sans rollback) dépendantes de leur ordre d'exécution ; ce dépôt a déjà payé ce prix côté frontend. On mutualise le coût, pas l'isolation. Composition plutôt qu'héritage pour la même raison : une classe de base abstraite aurait imposé sa hiérarchie à des suites qui mélangent `@DataJpaTest` et `@SpringBootTest`.
  **Conséquence NON anticipée, trouvée en exécutant** : la première suite complète après migration a rendu 83 erreurs de chargement de contexte — `FATAL: sorry, too many clients already`. Dix-huit conteneurs offraient 18 × 100 connexions ; un seul n'en offre que 100, alors que les contextes Spring sont mis en cache et gardent chacun leur pool Hikari. Corrigé par `max_connections=300` sur le conteneur partagé — en un seul endroit, ce qui est exactement le bénéfice recherché. Le plafond n'a délibérément PAS été obtenu en rabotant les pools : les suites de concurrence ont besoin de connexions réellement simultanées.
  **Second piège, trouvé en relisant le bytecode et non le code** : `PostgreSQLContainer` pose `fsync=off` dans son CONSTRUCTEUR (vérifié sur la 1.21.4 épinglée — pas dans `configure()`), si bien que `withCommand` l'écrasait. La suite restait verte ET plus rapide qu'en baseline, le gain sur les démarrages de conteneur masquant intégralement la régression : aucune assertion ne relisait ce réglage. D'où `PostgresTestSupportTest`, qui relit `max_connections`, `fsync` et `current_database()` **tels que le serveur les applique**. Prouvé par mutation : `fsync=off` retiré → ce test seul rougit (`expected: "off" but was: "on"`). C'est aussi le filet contre une future montée de version de Testcontainers qui changerait la forme de cette commande.
  Mesure, runs propres sur la même machine : **484 tests verts (481 existants + 3 du nouveau test de support), 2 min 21 contre 3 min 11 en baseline**, soit ≈ 26 % du temps de suite (les 2 min 31 relevés d'abord étaient sans `fsync=off`, et un run à 3 min 27 était contaminé par une suite frontend lancée en parallèle — mesure écartée). **Confirmé en CI** (run 30315313501 sur develop, 2026-07-28) : job Backend **1 min 29 contre 2 min 21** au run précédent (1303791), soit −37 % de bout en bout, checkout et mise en place du JDK compris ; `mvn verify` lui-même passe à 1 min 17 pour 484 tests, 0 saut. Le gain est bien plus net qu'en local, le runner ne disposant pas d'un cache d'image chaud. `PartnerKeyStoreTest`, qui portait son propre conteneur, tombe de 13,9 s à 0,57 s. Règle inscrite dans `project-context.md` : ne jamais déclarer de nouveau conteneur Postgres.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-10-anti-enumeration-ressources.md`
  summary: **Encodage et langue hétérogènes** dans les fichiers de test : plusieurs ne sont pas en UTF-8 valide, ce qui a conduit à écrire du français sans accents plutôt qu'à corriger la cause.
  evidence: `backend/src/test/java/com/zlecaf/escrow/service/EvidenceServiceTest.java` n'est pas en UTF-8 valide — invisible à `grep` sans `-a`, et `Files.readString(UTF_8)` y lève une `MalformedInputException` (le nouveau `AntiEnumerationConventionTest` lit donc les sources en ISO-8859-1 pour ne pas casser dessus : un contournement, pas un correctif). Conséquence observée à cette passe : les commentaires ajoutés dans plusieurs fichiers de test sont écrits en français **sans accents** pour ne pas aggraver l'encodage, ce qui dégrade la lisibilité et rend la langue du dépôt incohérente d'un fichier à l'autre. À rapprocher directement de l'entrée ESLint/lint déjà au ledger (Story 11-1) : c'est la même absence de barrière automatisée. Correctif = normaliser tous les fichiers en UTF-8 (une passe `iconv` vérifiable), puis un contrôle d'encodage dans la CI lite, au même endroit que le `lint`.
  status: RÉSOLU 2026-07-28 (bundle QUALITÉ-CI) — la cause (`EvidenceServiceTest.java`) avait été corrigée au balayage ; ce qui manquait était la **barrière**. Livrée : `scripts/check-encoding.py` (aucun octet NUL, UTF-8 valide, sur les 1893 fichiers suivis non déclarés binaires) + job CI dédié `hygiene`, et `.gitattributes` qui déclare explicitement les binaires — dénylist, pour qu'un type inconnu fasse ROUGIR au lieu d'être ignoré en silence. **Prouvé par mutation, trois branches** : littéral GIF à octets bruts réintroduit → détecté (offset, ligne, contexte) ; accent ISO-8859-1 → détecté (« invalid continuation byte ») ; les mêmes octets dans un fichier déclaré binaire → ignoré, ce qui prouve que l'exclusion vient bien de `.gitattributes`. Corollaire consigné dans `project-context.md` : le contournement « français sans accents » est levé, la barrière prouve désormais la validité UTF-8 à chaque push.
  **Résidu assumé, non traité :** 14 fichiers portent encore des commentaires en français sans accents (`propriete`, `litteral`, `meme`…), écrits sous l'ancien régime. Réécrire ces commentaires en masse serait de la churn pure — aucun comportement en jeu, et 14 fichiers de test touchés pour du cosmétique juste avant l'Epic 2. La règle étant écrite, ils se corrigeront au fil des passages ; la cause, elle, est fermée.

## Deferred from: code review de 2-1-fondation-ui-tokens-design-i18n (2026-07-28)

- source_spec: `_bmad-output/implementation-artifacts/2-1-fondation-ui-tokens-design-i18n.md`
  summary: La règle d'élévation de DESIGN.md (cartes bordées SANS ombre au repos ; ombre réservée aux surfaces flottantes) n'est pas appliquée aux composants préexistants.
  evidence: Huit emplacements portent `shadow-sm` ou `shadow-lg` sur des surfaces qui ne sont ni menu, ni toast, ni modale — `TransactionCard.vue:42` (plus `hover:shadow-md`), `TransactionDetailView.vue:134,204,209`, `RecoveryView.vue:225,260,294,321`, `AuthView.vue:83`. Relevé par l'auditeur d'acceptation de la revue 2.1, qui note que plusieurs de ces lignes jouxtent des hunks que le diff a modifiés pour le remap de couleurs : le sujet était sous les yeux et n'a pas été traité. L'AC1 dit « l'élévation respecte la règle » sans trancher entre « les tokens l'encodent » et « tous les écrans s'y conforment » ; côté tokens, la règle EST encodée (`--shadow-floating` unique, commentée comme réservée aux surfaces flottantes).
  status: OUVERT — **Story 2.2** (bibliothèque de composants de base), décision d'Oscard du 2026-07-28. Motif : 2.2 livre les composants Carte et les posera d'emblée sans ombre ; retoucher huit emplacements en 2.1 reviendrait à jeter une partie du travail quelques jours plus tard. À vérifier à la livraison de 2.2 : aucune carte au repos ne porte d'ombre, et la seule ombre du dépôt reste `--shadow-floating`.

- source_spec: `_bmad-output/implementation-artifacts/2-1-fondation-ui-tokens-design-i18n.md`
  summary: Six modules purs (`utils/`, `stores/`) portent encore des libellés anglais destinés à l'utilisateur, rendus par des composants pourtant migrés.
  evidence: `utils/evidence.js` (libellés de déposant + 4 messages de validation de fichier), `utils/frozenEntry.js` (3 noms d'action), `utils/replayFailure.js` (~10 motifs d'échec de synchronisation), `stores/auth.js` (2), `stores/escrow.js` (2), `stores/evidence.js` (1). Produit un écran MIXTE anglais/français sur `SyncFailureNotice` et `RecoveryView`, dont le texte voisin est traduit. Trouvés par la garde renforcée de la Story 2.1 après la revue — l'inventaire par regex qui l'avait précédée ne les voyait pas.
  status: OUVERT — dette NOMMÉE, tracée dans le code : la liste `PENDING_MIGRATION` de `src/i18n/__tests__/noHardcodedStrings.spec.js` énumère les six fichiers un par un, plutôt qu'un motif de répertoire qui se serait élargi tout seul. Retirer une entrée de cette liste fait ROUGIR la suite tant que le module n'est pas migré : c'est le test de non-régression de cette dette. Correctif identique pour tous — le module expose une CLÉ, la vue traduit (analogue frontend d'AD-23, déjà appliqué à `EVENT_LABEL_KEYS`) — mais il touche `FAILURE_LABELS`, dont la garde anti-dérive contre `ErrorCode.java` doit être préservée. Reporté délibérément en fin de revue 2.1 plutôt que bâclé.

## Deferred from: 2-3-layout-trois-espaces-navigation (2026-07-28)

- source_spec: `_bmad-output/implementation-artifacts/2-3-layout-trois-espaces-navigation.md`
  summary: L'AC5 demande un audit Lighthouse d'installabilité PWA ; ce qui est livré est un contrôle de manifeste, strictement moins large.
  evidence: `frontend/scripts/verify-pwa.mjs` vérifie les champs requis du manifeste, la présence des icônes ET leurs dimensions RÉELLES (lecture de l'en-tête PNG — une taille déclarée n'est pas une taille vérifiée), la cohérence des couleurs avec les tokens de `style.css`, et l'émission du service worker avec `index.html` au pré-cache. Il tourne en CI (`.github/workflows/ci.yml`, après le build) sans navigateur. Ce qu'il ne couvre PAS : HTTPS, portée effective du service worker à l'exécution, performances, et tout le reste de la grille Lighthouse. L'écart est déclaré en tête du script, dans le commentaire du job CI et dans les notes de complétion de la story — il n'a jamais été présenté comme une équivalence.
  status: OUVERT — la story est close `done` avec cette dette assumée par Oscard le 2026-07-28. Piste : job CI dédié avec `treosh/lighthouse-ci-action` sur un `vite preview`, ou passage manuel documenté. À trancher au moment où la PWA devient une promesse client et non un support de développement.

- source_spec: `_bmad-output/implementation-artifacts/2-3-layout-trois-espaces-navigation.md`
  summary: L'affichage hors réseau n'a jamais été essayé dans un vrai navigateur — il est déduit du contenu du pré-cache.
  evidence: La tâche T6 demandait de « précacher le shell et vérifier son affichage sans réseau ». Le premier volet est fait (`globPatterns` explicite dans `vite.config.js`, plutôt que le motif par défaut du plugin, qui n'est pas un contrat) et `verify-pwa.mjs` échoue si `index.html` n'apparaît pas dans `dist/sw.js`. Le second volet ne l'est pas : aucun rendu réel n'a été observé réseau coupé. La présence d'un fichier au pré-cache et son affichage effectif sont deux choses différentes — c'est le même écart que celui qui avait rendu la sentinelle `verify:no-demo` creuse en Story 2.2, où un contrôle passait alors que la galerie était bel et bien dans le bundle.
  status: OUVERT — même arbitrage que l'item précédent. Se solde naturellement avec le harnais Lighthouse (mode hors ligne), ou par un test Playwright coupant le réseau sur `vite preview`. Aucune urgence tant que l'app n'est pas installée par de vrais utilisateurs, mais l'AC ne doit pas être relue comme satisfaite.

## Deferred from: 2-7-politique-session-appareil-partage (2026-08-11)

- source_spec: `_bmad-output/implementation-artifacts/2-7-politique-session-appareil-partage.md`
  summary: La garde anti-chaînes en dur ne scanne plus AUCUN fichier `.ts` — son volet « prose dans le code » est mort en silence depuis la migration TypeScript, et sa liste d'exemption nomme six fichiers qui n'existent plus.
  evidence: `frontend/src/i18n/__tests__/noHardcodedStrings.spec.ts:236` globe `**/*.{vue,js}`. Vérifié le 2026-08-11 : depuis les commits `47c075f`→`35e1a0c` (migration TS, survenue entre les Stories 2-3 et 2-4), les 49 modules et suites du dépôt sont en `.ts` — ce glob ne lit donc plus rien du côté « script et modules ». Le volet GABARIT (fichiers `.vue`) fonctionne toujours, ce qui explique que personne ne l'ait vu : la garde n'est pas silencieuse, elle est **à moitié** silencieuse. Corollaire : la liste `PENDING_MIGRATION` de la Story 2.1, qui énumère `utils/evidence.js`, `utils/frozenEntry.js`, `utils/replayFailure.js`, `stores/auth.js`, `stores/escrow.js`, `stores/evidence.js`, ne désigne plus aucun fichier existant — l'exemption ET son test de non-régression sont donc morts ensemble. C'est la CINQUIÈME garde creuse trouvée sur cet epic, et la première qui l'est devenue par un RENOMMAGE plutôt que par une erreur de conception. Elle protège un invariant du spine (AD-23 : « clé manquante = échec CI »).
  status: OUVERT — décision d'Oscard du 2026-08-11 : au registre, pas de réparation dans la Story 2.7. Motif : réparer le glob ferait très probablement rougir la suite sur-le-champ, et la Story 2.7 s'interdit explicitement d'ajouter des entrées à `PENDING_MIGRATION` pour se calmer. La réparation demande sa propre passe, en trois temps : (1) corriger le glob en `**/*.{vue,ts}` ; (2) recenser ce qui est passé sous le radar depuis la migration — c'est le vrai livrable, personne ne sait aujourd'hui combien de chaînes littérales sont entrées dans les modules `.ts` ; (3) renommer les six entrées de `PENDING_MIGRATION` en `.ts` et vérifier par MUTATION que retirer une entrée fait bien rougir, ce qui n'est plus le cas aujourd'hui. À faire avant toute story qui livre de la prose dans un module, et au plus tard avec la Story 11-7 (dette de tests).

- source_spec: `_bmad-output/implementation-artifacts/2-7-politique-session-appareil-partage.md`
  summary: Le « canal d'annonces a11y centralisé » exigé par le spine n'existe pas ; chaque écran pose sa propre région `aria-live`.
  evidence: La convention Frontend du spine 2026-07-24 (ligne 188) impose un « canal d'annonces a11y centralisé (store unique alimentant les régions `aria-live` : transitions, upload, online/offline, rejeu — UX-DR39) », plancher WCAG 2.2 AA. Vérifié le 2026-08-11 : `grep -rn "aria-live" frontend/src` ne renvoie qu'UNE occurrence, locale à `VerifyEmailView.vue:148`. Aucun store, aucun module partagé. La Story 2.7 avait besoin d'annoncer deux événements — le motif d'expiration d'inactivité et la fin de session propagée entre onglets — et a repris l'idiome local (`role="status"`) plutôt que d'inventer le canal manquant : le créer aurait été une refonte a11y transverse, sans rapport avec le périmètre de la story et sans les autres émetteurs que la convention énumère (transitions, upload, online/offline, rejeu).
  status: OUVERT — décision d'Oscard du 2026-08-11 : au registre. Note de portée : UX-DR39 énumère quatre familles d'événements annoncés et **ne cite pas la fin de session** ; l'extension de cette liste est à acter en même temps que la création du canal. Candidats d'atterrissage : une story de l'Epic 9 (le rejeu de file est l'un des émetteurs listés et n'a pas non plus son annonce), ou une passe a11y dédiée adossée à la dette AC5 Lighthouse de la Story 2-3, qui pose déjà la question du plancher WCAG vérifié pour de vrai plutôt que déduit.

- source_spec: `_bmad-output/implementation-artifacts/2-7-politique-session-appareil-partage.md`
  summary: `AppButton` (Story 2-2) porte une garde anti-second-clic EN DOUBLE, et la moitié JavaScript n'est falsifiable par aucune mutation.
  evidence: Mesuré le 2026-08-11 pendant la passe T10 de la Story 2.7. Le composant garde deux fois contre le même événement : l'attribut `:disabled="disabled || pending"` sur le `<button>`, et un `if (props.disabled || props.pending) return` en tête d'`onClick`. Les deux mutations ont été appliquées séparément sur la suite ENTIÈRE (550 tests). Retirer l'attribut → **6 rouges**, dont `désactive réellement le bouton, pas seulement visuellement` et `n'a lancé qu'UNE terminaison` : cette moitié est prouvée. Retirer la garde JavaScript → **0 rouge** : elle n'est falsifiable par aucun test, et elle ne peut pas l'être, pour une raison structurelle — un `<button disabled>` n'émet pas de `click` dans un navigateur, et `wrapper.trigger('click')` de `@vue/test-utils` refuse pareillement de cliquer un élément désactivé (constat déjà consigné dans les notes de complétion T5 de la 2.7). Le chemin gardé est donc inatteignable aussi longtemps que l'attribut reste posé. C'est exactement le motif que la Story 2.7 a refusé en T5 sur `DashboardView` — « une garde improuvable en double d'une garde prouvée n'est pas de la défense en profondeur, c'est une preuve creuse en attente d'être citée » — mais dans un composant que la 2.7 n'a pas écrit.
  status: OUVERT — SIGNALÉ, PAS CORRIGÉ, et le choix est délibéré. La Story 2.7 a amendé `AppButton` de façon strictement ADDITIVE (anneau de focus, `labelParams`), avec l'accord explicite du PO sur ce périmètre ; retirer une garde existante est un changement de comportement, pas un ajout, et il appartient au propriétaire de la 2-2. Deux atterrissages possibles, à trancher : (a) retirer la garde JavaScript, en actant que `:disabled` est la garde unique et prouvée ; (b) la conserver et la rendre falsifiable — ce qui suppose de décider qu'`AppButton` doit rester correct même si un futur usage remplace `disabled` par `aria-disabled`, cas où le clic redevient émis et où cette garde cesse d'être un double. L'option (b) est la seule qui donne à cette ligne une raison d'exister ; elle demande alors son test. À trancher avec le prochain travail sur la bibliothèque de composants.
